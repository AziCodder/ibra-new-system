name: CI

on:
  push:
    branches: [main, master, auto/ibra-dev]
  pull_request:
    branches: [main, master, auto/ibra-dev]

jobs:
  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ibra_orders_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10

    env:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/ibra_orders_test
      SESSION_SECRET: ci-test-secret
      UPLOAD_DIR: ./uploads

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install backend dependencies
        working-directory: backend
        run: pip install -r requirements.txt

      - name: Run migrations
        working-directory: backend
        run: python -m alembic upgrade head

      - name: Ruff
        working-directory: backend
        run: python -m ruff check .

      - name: Pytest
        working-directory: backend
        run: python -m pytest tests/ -v --tb=short

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Typecheck
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Lint
        working-directory: frontend
        run: npx eslint src/ --ext .ts,.tsx

  # Сборка боевых образов. Живёт здесь, а не отдельным workflow, чтобы
  # `needs` физически не давал опубликовать образ, не прошедший тесты.
  #
  # Зачем вообще собирать в CI: на сервере 2 ГБ памяти, а `vite build`
  # требует 1.5-2.5 ГБ. Сборка на боевой машине падала бы по OOM ровно в
  # момент выкладки. Сервер только скачивает готовый образ.
  images:
    needs: [backend, frontend]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Два тега на образ: latest и короткий SHA. latest — чтобы сервер
      # тянул «свежее», SHA — чтобы можно было откатиться на конкретную
      # сборку, не гадая, какая из них была вчера.
      - name: Backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile.prod
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ibra-backend:latest
            ghcr.io/${{ github.repository_owner }}/ibra-backend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          file: ./frontend/Dockerfile.prod
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/ibra-frontend:latest
            ghcr.io/${{ github.repository_owner }}/ibra-frontend:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Показать, что выкладывать
        run: |
          echo "Образы опубликованы. На сервере:"
          echo "  IMAGE_TAG=${{ github.sha }} docker compose -f docker-compose.prod.yml pull"
          echo "  IMAGE_TAG=${{ github.sha }} docker compose -f docker-compose.prod.yml up -d"
