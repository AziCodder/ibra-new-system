# QA-тесты: ручная проверка правок от 15.08.2026

**Что проверяем:** 14 коммитов от `11a9bac` (12.08) до `2468198` (15.08) — реализация требований клиента из [ПРАВКИ_КЛИЕНТА_2026-08-13.md](ПРАВКИ_КЛИЕНТА_2026-08-13.md) по [ТЗ_ПРАВКИ_2026-08-15.md](ТЗ_ПРАВКИ_2026-08-15.md).

| Коммит | Что изменилось | Блок тестов |
|---|---|---|
| `2e7b605` | курс от валюты записи к валюте заказа + миграция + шаги стрелочек + реквизиты | A, B, C |
| `bfd08ea` | фильтр по остатку на «Оплатах» | D |
| `c314a67` | заявки отменённых заказов скрыты | D |
| `7290ed3` | левая панель следует светлой теме | F |
| `a67db32` | убрана валюта EUR | E |
| `0cc7744` | валюта нового заказа по умолчанию RUB | E |
| `329120e` | админ сам привязывает телеграм-чаты к клиентам | G |
| `17450eb` | уведомление `#требуетсяоплата` + `PUBLIC_BASE_URL` + отправка файлов | H |
| `af932b0` | уведомление `#прошлаоплата` | H |
| `8d53579` | уведомление `#грузвыехал` | H |
| `f150aa5` | уведомление `#встречайтегруз` | H |
| `9526f08` | сбой уведомления не роняет сохранённую операцию | H |
| `602b4d6`, `2468198` | техдолг (импорты, линт) | I |

Легенда приоритетов: 🔴 блокирующий · 🟡 важный · ⚪ косметика.

---

## 0. 🔴 БЛОКЕР: база работающего приложения отстала от миграций

**Обнаружено при подготовке этого плана 15.08.2026.** Касается **только ручного тестирования через интерфейс**. Автотесты в порядке — они ходят в отдельную базу `ibra_test`, которая на head (проверено: **317 passed**).

Приложение на http://localhost:5173 работает с базой `ibra_orders`, и с ней проблема:

```
$ docker compose exec backend alembic current
FAILED: Can't locate revision identified by 'f4c3d0e6f923'
```

В `alembic_version` базы `ibra_orders` стоит ревизия `f4c3d0e6f923`, которой **нет в репозитории** — она осталась от заброшенной ветки `backup/multicurrency` (коммит `1845c53`, «перед сбросом до origin»). Alembic не находит точку отсчёта и не применяет ничего дальше.

| Что | `ibra_test` (автотесты) | `ibra_orders` (приложение) |
|---|---|---|
| Ревизия | `c58a1e7b3d94` — head ✅ | `f4c3d0e6f923` — фантом ❌ |
| `telegram_groups.is_active` | есть ✅ | **нет** ❌ |
| Разворот курса `f6b2d90a4c17` | применён ✅ | **не применён** ❌ |

Что сломается при ручном прогоне без починки:
- **Блок G целиком** — раздел «Телеграм-чаты» упадёт с ошибкой: `column telegram_groups.is_active does not exist`.
- **Блок H** — уведомления не найдут привязанные чаты.
- **Блок A на старых записях** — заказы, созданные до 15.08, покажут неверные итоги: код уже умножает, а курсы в базе остались в старом направлении. На **новых** записях (весь «золотой заказ» из §2) всё будет корректно.

### Как чинить

Пересоздать `ibra_orders` — в ней только фикстуры, ценных данных нет:

```bash
docker compose down -v && docker compose up -d && docker compose exec backend alembic upgrade head && docker compose exec backend python -m app.scripts.create_admin
```

**Чего делать НЕ надо:** `alembic stamp f6b2d90a4c17` + `upgrade head`. Ошибка уйдёт, но alembic сочтёт разворот курса выполненным, и старые записи останутся со старым направлением — блок A будет врать, и будет непонятно, дефект это или недоприменённая миграция.

### ⚠️ Проверить боевую базу отдельно

Если фантомная ревизия стоит и на проде — при деплое разворот курса **не применится**, и правка №1 клиента останется несделанной, хотя код будет новый. Получится худший вариант: код умножает, данные в старом направлении.

```bash
# на прод-сервере
docker compose exec backend alembic current
```

Если результат — `Can't locate revision`, разбираться **до** деплоя.

---

## 1. Подготовка стенда

```bash
docker compose up -d
```

| Что | Где |
|---|---|
| Фронтенд | http://localhost:5173 |
| Бэкенд / Swagger | http://localhost:8011/docs |
| БД | `localhost:5433`, `postgres/postgres`, база `ibra_orders` |

**Проверить, что миграции применились** (обе новые обязательны):

```bash
docker compose exec backend alembic current
```

Ожидается head-ревизия, в цепочке которой есть `f6b2d90a4c17` (разворот курса) и `c58a1e7b3d94` (флаг активности чата).

**Доступ в БД для сверки:**

```bash
docker compose exec db psql -U postgres -d ibra_orders
```

**Учётные записи:** нужны три роли — `admin`, `manager`, `observer`. Если их нет:

```bash
docker compose exec backend python -m app.scripts.create_admin
```

### Как смотреть уведомления

Два режима, оба валидны:

**Режим 1 — без бота (быстрый, для проверки текста).** Если `TELEGRAM_BOT_TOKEN` не задан, сообщение не уходит и в БД не пишется, но текст целиком попадает в лог:

```bash
docker compose logs -f backend | grep "NOTIFY ->"
```

**Режим 2 — с ботом (полный, для проверки доставки и файлов).** Задать `TELEGRAM_BOT_TOKEN` и `PUBLIC_BASE_URL`, перезапустить бэкенд. Результат каждой отправки виден в интерфейсе: **База данных → Уведомления**.

> ⚠️ Блок H целиком проверяется в **режиме 2**; в режиме 1 доступны только кейсы про текст (H-01…H-04, H-09, H-10).

> ⚠️ Фронтенд в Docker не всегда подхватывает правки с хоста. Если изменения не видны — `docker restart ibranewsystem-frontend-1` и жёсткая перезагрузка страницы (Ctrl+Shift+R).

---

## 2. Тестовые данные — «золотой заказ»

Создать один раз, на нём гоняется большинство кейсов. Все числа ниже — эталонные, они же ожидаемые результаты.

| Шаг | Что создать | Значения |
|---|---|---|
| 1 | Клиент | `QA Клиент` |
| 2 | Заказ | валюта **RUB** |
| 3 | Товар «Дрон А» | кол-во **10**, цена **100**, валюта **CNY**, курс **12** |
| 4 | Товар «Кабель Б» | кол-во **5**, цена **200**, валюта **RUB** (курс скрыт) |
| 5 | Запрос на оплату | по «Дрон А» на **1000 CNY** |
| 6 | Оплата по запросу | **400 CNY**, валюта **CNY**, курс **1** |
| 7 | Отправка (логистика) | трекинг `QA-TRACK-1`, дата сегодня |
| 8 | Приёмка логистики | расход **50**, валюта **CNY**, курс **12** |
| 9 | Расход | **100**, валюта **CNY**, курс **12** |
| 10 | Доход | **20 000**, валюта **RUB** |

**Контрольные суммы:**

| Показатель | Значение |
|---|---|
| Сумма «Дрон А» | 1 000 CNY = **12 000 RUB** |
| Сумма «Кабель Б» | **1 000 RUB** |
| Итог по товарам в валюте заказа | **13 000 RUB** |
| Остаток по запросу после оплаты | **600 CNY** |
| Закупки | 400 × 1 × 12 = **4 800 RUB** |
| Логистика | 50 × 12 = **600 RUB** |
| Прочие расходы | 100 × 12 = **1 200 RUB** |
| Доходы | **20 000 RUB** |
| Прибыль | 20 000 − 4 800 − 600 − 1 200 = **13 400 RUB** |

---

## 3. Блок A. Направление курса 🔴

> Главное требование клиента. Правило: **курс = сколько единиц валюты заказа в 1 единице валюты записи**, пересчёт — **умножение**.

### A-01 · 🔴 Подпись поля курса при добавлении товара
1. Заказ в RUB → «Добавить товар».
2. Валюта товара → **CNY**.

**Ожидается:** подпись поля — **«Курс: 1 CNY = ? RUB»**.
**Провал:** «1 RUB = ? CNY» — направление не развернули.

### A-02 · 🔴 Превью пересчёта товара
1. В той же форме: количество **10**, цена **100**, курс **12**.

**Ожидается:** подсказка под полем курса — **«1 000 CNY ≈ 12 000 RUB»**.
**Провал:** «≈ 83,33 RUB» — осталось деление.

### A-03 · 🔴 Курс в расходе
1. Вкладка учёта → «Добавить расход»: сумма **100**, валюта **CNY**, курс **12**.

**Ожидается:** подпись «Курс: 1 CNY = ? RUB», превью **«100 CNY ≈ 1 200 RUB»**.
**Провал:** «≈ 8,33 RUB» — это исходный баг со скриншота клиента `photo_193`.

### A-04 · 🔴 Курс в доходе
То же, что A-03, для «Добавить доход».

### A-05 · 🔴 Курс в приёмке логистики
1. Открыть отправку → «Приёмка логистики»: сумма **50**, валюта **CNY**, курс **12**.

**Ожидается:** подпись **«Курс: 1 CNY = ? RUB»**, в расходах заказа — **600 RUB**.
**Провал:** «1 RUB = ? CNY» — на скриншотах клиента `photo_186`/`photo_189` было именно так.

### A-06 · 🟡 Курс 1:1 при совпадении валют
1. В любой из форм выбрать валюту, равную валюте заказа (**RUB**).

**Ожидается:** поле курса **исчезает**, запись сохраняется, в итогах сумма идёт как есть без пересчёта.
**Проверить во всех формах:** товар, расход, доход, приёмка логистики.

### A-07 · 🔴 Итоги и прибыль по заказу
1. Открыть «золотой заказ» после всех шагов из §2.

**Ожидается:** все контрольные суммы из §2 совпадают, прибыль **13 400 RUB**.
**Примечание:** блок прибыли считается готовым только когда весь товар принят по логистике; если показывает «не готово» — это не баг, доотправить остаток.

### A-08 · 🔴 Миграция старых данных
Самый рискованный кейс: миграция `f6b2d90a4c17` инвертирует все курсы в БД.

> ⚠️ **Только на копии боевой базы.** На dev-базе кейс не имеет смысла: после пересоздания из §0 там не остаётся данных, созданных до правки. Порядок: снять дамп прода → развернуть локально → зафиксировать итоги нескольких заказов **до** миграции → `alembic upgrade head` → сверить.

1. Найти запись, созданную **до 15.08**:
   ```sql
   SELECT id, amount, currency, exchange_rate, created_at
   FROM ledger_entries ORDER BY id LIMIT 20;
   ```
2. То же для `products`, `payments`, `logistics`.

**Ожидается:**
- Курс читается «по-человечески»: для записи в CNY на заказе в RUB это **~12**, а не **~0,083**.
- Курсы, равные `1`, остались `1`.
- Итоги старых заказов на экране **не изменились** относительно того, что было до правок.

**Как сверить итоги:** если есть скриншоты или выгрузка прибыли до 15.08 — сравнить. Если нет — взять заказ, где все валюты совпадают с валютой заказа (все курсы = 1): его итоги миграция затронуть не могла, они должны быть точно те же.

### A-09 · 🟡 Отображение курса в списках и карточках
1. Посмотреть курс в: таблице товаров, списке доходов/расходов, карточке товара, карточке записи, карточке оплаты, карточке логистики.

**Ожидается:** везде формат **«1 CNY = 12 RUB»** (валюта записи первой). Нигде не осталось обратной записи.

---

## 4. Блок B. Шаг стрелочек в числовых полях 🟡

> Финальные значения клиента от 13.08, 15:44. Раньше во всех полях был `step="any"` и `min="0.000001"`, из-за чего стрелка давала `4,000001`.

### B-01 … B-04 · 🟡 Шаг по формам

Для каждой строки: очистить поле, нажать стрелку **вверх один раз**, затем ещё раз.

| # | Форма | Поле | Ожидается после 1-го и 2-го нажатия |
|---|---|---|---|
| B-01 | Добавить товар | Количество | `1`, затем `2` |
| B-01 | Добавить товар | Цена | `0.1`, затем `0.2` |
| B-02 | Внести оплату | Сумма | `1`, затем `2` |
| B-02 | Внести оплату | Курс | `0.01`, затем `0.02` |
| B-03 | Приёмка логистики | Сумма | `1`, затем `2` |
| B-03 | Приёмка логистики | Курс | `0.01`, затем `0.02` |
| B-04 | Доход / Расход | Сумма | `1`, затем `2` |
| B-04 | Доход / Расход | Курс | `0.01`, затем `0.02` |

**Провал:** появляется `0,000001` или `1,000001` — не поправили `min`.

### B-05 · 🟡 Ручной ввод дробных значений не сломался
1. В поле «Сумма» (шаг 1) ввести руками **1234.56**.
2. В поле «Курс» (шаг 0.01) ввести руками **12.417**.
3. Сохранить.

**Ожидается:** обе записи сохраняются, значения не округляются и не отклоняются.
**Почему важно:** явный `step` делает такие значения «невалидными» с точки зрения HTML. Сохранение здесь идёт по `onClick`, а не нативной отправкой формы, поэтому блокировать не должно — но проверить обязательно.

### B-06 · ⚪ Стрелки есть везде
Пройти по всем полям из таблицы и убедиться, что стрелки вверх/вниз отображаются (требование клиента от 13.08, 09:17).

---

## 5. Блок C. Поле «Реквизиты» 🟡

### C-01 · 🟡 Многострочный ввод
1. «Создать запрос на оплату» → поле «Реквизиты».

**Ожидается:** это **многострочное поле** (как «Детали платежа»), а не однострочная полоска. Enter создаёт новую строку.

### C-02 · 🟡 Длинная строка без пробелов
1. Вставить реальные реквизиты клиента:
   ```
   周斯丽6217007200052740738中国建设银行股份有限公司深圳上步支行
   ```

**Ожидается:** строка **переносится** внутри поля и видна целиком; поле прокручивается вертикально; текст не уезжает вбок.
**Провал:** горизонтальная прокрутка и обрезанный текст — исходная жалоба клиента (`photo_188`, `photo_194`).

### C-03 · 🟡 Отображение после сохранения
1. Сохранить запрос, открыть его карточку.

**Ожидается:** реквизиты видны целиком, вёрстка панели не разъезжается по горизонтали, переносы строк сохранены.
2. Проверить, что та же строка целиком попала в уведомление `#требуетсяоплата` (см. H-01).

---

## 6. Блок D. Страница «Оплаты» 🟡

### D-01 · 🟡 Фильтр по остатку — значение по умолчанию
1. Оплатить какой-нибудь запрос **полностью** (остаток 0).
2. Открыть страницу «Оплаты» заново.

**Ожидается:** фильтр остатка стоит в **«> 0»**, полностью оплаченная заявка **не видна**.

### D-02 · 🟡 Переключение на «Все»
1. Переключить фильтр в «Все».

**Ожидается:** заявка с нулевым остатком появляется. Обратное переключение снова её скрывает.

### D-03 · 🟡 Заявки отменённых заказов скрыты
1. Перевести «золотой заказ» в статус **Отменён**.
2. Открыть «Оплаты».

**Ожидается:** заявки этого заказа **не видны ни при каком** значении фильтра остатка — клиент просил убрать «вообще».

### D-04 · 🟡 Внутри заказа заявки остались
1. Открыть сам отменённый заказ → вкладка запросов на оплату.

**Ожидается:** заявки на месте. Скрытие работает только на общей странице «Оплаты».

### D-05 · 🟡 Проверка под ролью менеджера
1. Войти менеджером, открыть «Оплаты».

**Ожидается:** видны только заявки своих заказов; фильтр остатка и скрытие отменённых работают так же.

### D-06 · ⚪ API напрямую
```
GET /api/payment-requests/?remaining=positive   → только с остатком (по умолчанию)
GET /api/payment-requests/?remaining=all        → все
```
Заявки отменённых заказов не должны приходить ни в одном из вариантов.

---

## 7. Блок E. Валюты 🟡

### E-01 · 🟡 EUR отсутствует в выборе
Проверить выпадающие списки валют в: создании заказа, добавлении товара, доходе/расходе, приёмке логистики, внесении оплаты.

**Ожидается:** доступны только **USD, CNY, RUB**. EUR нет нигде.

### E-02 · 🔴 Старые записи в EUR читаются
1. Найти или создать в БД запись с валютой EUR:
   ```sql
   SELECT id, currency FROM products WHERE currency = 'EUR';
   SELECT id, currency FROM orders WHERE currency = 'EUR';
   ```
2. Открыть соответствующий заказ в интерфейсе.

**Ожидается:** заказ и товар **открываются без ошибок**, сумма считается, символ `€` отображается.
**Почему важно:** EUR намеренно оставлен в `Currency`, но убран из `CurrencyIn`. Если бы его удалили из обоих — старые записи падали бы с 500. На `photo_196` клиента такой товар был.

### E-03 · 🟡 API отклоняет создание в EUR
Через Swagger отправить создание товара с `"currency": "EUR"`.

**Ожидается:** **422**, а не 201.

### E-04 · 🟡 Валюта нового заказа — RUB
1. «Создать заказ».

**Ожидается:** в поле валюты уже стоит **RUB** (было USD).
2. Через API создать заказ, не передав `currency`.

**Ожидается:** заказ создан с **RUB**.

---

## 8. Блок F. Тема оформления 🟡

### F-01 · 🟡 Левая панель в светлой теме
1. Переключить тему на светлую (кнопка внизу сайдбара).

**Ожидается:** сайдбар становится **серым, чуть темнее фона страницы** (фон `#eef1f5`, панель `#e3e7ee`). Текст пунктов тёмный и читаемый, активный пункт — синий с белым текстом.
**Провал:** панель осталась тёмно-синей — исходная жалоба клиента (`photo_198`).

### F-02 · 🟡 Переключение без перезагрузки
1. Переключить тему туда-обратно несколько раз, не перезагружая страницу.

**Ожидается:** сайдбар меняет цвет **сразу**, без «залипания» старых цветов.
2. Перезагрузить страницу — тема сохранилась.

### F-03 · 🟡 Тёмная тема не пострадала
1. Вернуться в тёмную тему.

**Ожидается:** сайдбар выглядит как раньше — цвета тёмной темы менять не планировалось.

### F-04 · ⚪ Мелочи сайдбара в светлой теме
Проверить читаемость: аватар пользователя, подпись роли, кнопка «Выйти», ховер по пунктам, разделители, мобильная версия (узкое окно).

---

## 9. Блок G. Привязка Telegram-чатов 🟡

> Реализован «Вариант 2» клиента: админ добавляет бота в чат, чат появляется в системе сам, дальше админ связывает его с клиентами. Раздел: **База данных → Телеграм-чаты**.

### G-01 · 🟡 Чат регистрируется сам
1. Создать в Telegram группу, добавить туда бота.
2. Открыть «База данных → Телеграм-чаты».

**Ожидается:** группа появилась в списке с названием и `chat_id`, помечена активной. Ручного ввода не потребовалось.

### G-02 · 🟡 Привязка к нескольким клиентам
1. Отметить у чата **двух** клиентов, сохранить.
2. Перезагрузить страницу.

**Ожидается:** обе привязки сохранились. Один чат может обслуживать нескольких клиентов — прямое требование клиента.

### G-03 · 🟡 Снятие привязки
1. Снять отметку с одного клиента, сохранить.

**Ожидается:** остался только второй; уведомления по отвязанному клиенту в этот чат больше не идут (проверить вместе с H-06).

### G-04 · 🟡 Бот удалён из чата
1. Удалить бота из группы.
2. Обновить список чатов.

**Ожидается:** чат помечен **неактивным**. Уведомления по привязанным к нему клиентам туда не отправляются — при отсутствии других чатов срабатывает запасной канал (H-07).

### G-05 · ⚪ Удаление чата из системы
1. Удалить чат кнопкой удаления.

**Ожидается:** чат и все его привязки исчезли. Если бот всё ещё в группе — при следующем событии чат зарегистрируется заново (это нормально).

### G-06 · 🟡 Доступ только админу
1. Войти менеджером и наблюдателем.

**Ожидается:** вкладка «Телеграм-чаты» недоступна; прямые запросы к `/api/telegram-groups/` дают **403**.

---

## 10. Блок H. Уведомления 🟡

> Общие правила: хештег — **первой строкой**; ссылка абсолютная, из `PUBLIC_BASE_URL`; файлы уходят следом за сообщением.

### H-01 · 🟡 `#требуетсяоплата`
1. Создать запрос на оплату: приоритет **Обычно**, реквизиты — длинная китайская строка из C-02, детали заполнить, итого **16 000 CNY**.

**Ожидается — текст ровно такой структуры:**
```
#требуетсяоплата

Запрос на оплату #97
Приоритет: Обычно

Реквизиты: 周斯丽6217007200052740738中国建设银行股份有限公司深圳上步支行

Детали: <текст>
Итого: 16 000 CNY

Ссылка: https://<PUBLIC_BASE_URL>/orders/420
```
Проверить: хештег первой строкой, пустая строка после него, сумма с пробелом-разделителем разрядов, реквизиты целиком.

### H-02 · 🟡 `#прошлаоплата` и остаток
1. По запросу на **1000 CNY** внести оплату **400 CNY**, курс **1**, примечание заполнить, приложить файл.

**Ожидается:**
```
#прошлаоплата

Оплата по запросу #97

Сумма: 400 CNY
Курс: 1
Остаток после оплаты: 600 CNY

Примечание: <текст>
Ссылка: https://<PUBLIC_BASE_URL>/orders/420
```
**Ключевая проверка:** «Остаток после оплаты» = **600 CNY**, то есть уже **с учётом** этой оплаты, а не до неё.
2. Внести вторую оплату **600 CNY** → остаток должен стать **0 CNY**.

### H-03 · 🟡 `#грузвыехал` — отправляется автоматически
1. Создать отправку (логистику) со статусом «в пути»: трекинг, дата, примечание.

**Ожидается:** уведомление уходит **сразу при создании**, без нажатия кнопки:
```
#грузвыехал

Дата отправки: 15.08.2026
Трекинг: QA-TRACK-1
Примечание: <текст>
Ссылка: https://<PUBLIC_BASE_URL>/orders/420
```
2. **Негативный кейс:** создать логистику сразу со статусом «принято» (запись задним числом).

**Ожидается:** уведомление **не отправляется** — это не отправка, а фиксация давно приехавшего груза.

### H-04 · 🟡 `#встречайтегруз` — по кнопке
1. Открыть отправку → кнопка «Уведомить о получении».

**Ожидается:**
```
#встречайтегруз

Трекинг: QA-TRACK-1
Примечание: <текст>
Ссылка: https://<PUBLIC_BASE_URL>/orders/420
```

### H-05 · 🟡 Файлы прикладываются
1. Создать запрос на оплату **с двумя вложениями**.
2. Внести оплату **с приложенным чеком**.

**Ожидается:** в чат приходит сначала текст, затем файлы. Имена файлов не искажены.
3. Повторить без вложений — приходит только текст, ошибок нет.

### H-06 · 🟡 Отправка в несколько чатов
1. Привязать клиента к **двум** чатам (G-02).
2. Создать запрос на оплату.

**Ожидается:** уведомление пришло **в оба** чата; в «База данных → Уведомления» две записи со статусом «отправлено».

### H-07 · 🟡 Запасной канал — личный чат
1. Отвязать клиента от всех чатов, но оставить заполненным `telegram_chat_id`.
2. Создать запрос на оплату.

**Ожидается:** уведомление ушло в личный чат.
3. Убрать и его → операция всё равно проходит успешно, уведомление просто не уходит.

### H-08 · 🔴 Сбой уведомления не роняет операцию
Ключевой кейс коммита `9526f08`.

1. Сломать отправку: указать заведомо неверный `TELEGRAM_BOT_TOKEN` (или привязать чат с несуществующим `chat_id`).
2. Внести оплату по запросу.
3. Создать отправку.

**Ожидается:**
- Операция **сохраняется**, пользователь видит успех, а **не ошибку**.
- Оплата видна в истории, остаток пересчитан.
- В логе бэкенда — запись об ошибке уведомления, в «Уведомления» — статус «не доставлено».

**Почему критично:** если операция откатится, оператор решит, что платёж не прошёл, и внесёт его повторно — двойной учёт денег.

### H-09 · 🟡 Абсолютная ссылка
1. Задать `PUBLIC_BASE_URL=https://example.test`, перезапустить бэкенд, вызвать любое уведомление.

**Ожидается:** ссылка вида `https://example.test/orders/42`, кликабельна. Не должно быть относительного `/orders/42` и двойных слешей.

### H-10 · ⚪ Формат чисел
**Ожидается:** сумма `16000` → `16 000`; `1234.5` → `1 234,5`; курс `12.000000` → `12`; курс `12.410000` → `12,41`. Лишних нулей нет.

### H-11 · ⚪ Журнал уведомлений
1. «База данных → Уведомления».

**Ожидается:** видны все отправки этой сессии с получателем, текстом, статусом и текстом ошибки для неудачных.

---

## 11. Блок I. Регресс 🔴

### I-01 · 🔴 Автотесты

Запускать **обязательно с указанием тестовой базы** — по умолчанию `DATABASE_URL` ведёт в `ibra_orders`, а она отстала (см. §0), и прогон даёт 43 ложных падения:

```bash
docker compose exec -e DATABASE_URL="postgresql+asyncpg://postgres:postgres@db:5432/ibra_test" backend pytest -q
```

**Эталон на 15.08: `317 passed`.** Любое падение — настоящий дефект.
Особое внимание: `test_profit.py`, `test_key_logic.py`, `test_payments.py`, `test_products.py`, `test_notification_templates.py`, `test_telegram_groups.py`, `test_logistics_crud.py`.

### I-02 · 🔴 Сборка фронтенда
```bash
docker compose exec frontend npm run build
```
**Ожидается:** сборка без ошибок TypeScript.

### I-03 · 🟡 Сквозной сценарий клиента
Пройти «золотой заказ» из §2 целиком, от создания клиента до прибыли, за один заход. Ошибок и пустых экранов быть не должно, все контрольные суммы совпадают.

### I-04 · 🟡 Роли
Под `manager` и `observer` пройти основные экраны.
**Ожидается:** наблюдатель ничего не создаёт и не редактирует; менеджер видит только свои заказы; вкладки админа скрыты.

### I-05 · 🟡 Существующие заказы
Открыть 3–5 заказов, созданных **до 15.08**.
**Ожидается:** открываются, считаются, курсы читаются в новом направлении, итоги совпадают с тем, что было до правок.

---

## 12. Что НЕ реализовано — не заводить как баги

Эти пункты из ТЗ в данной итерации не делались. Если QA на них наткнётся — это известный объём, а не дефект.

| Пункт ТЗ | Состояние | Что увидит тестировщик |
|---|---|---|
| **1.3** Убрать выбор валюты в «Внести оплату» | не сделано | В форме оплаты остался выпадающий список валют, курс пересчитывает в валюту **запроса**, а не заказа |
| **1.4** Курс покупки валюты на каждой оплате | не сделано | Следствие 1.3: сценарий клиента «50 000 CNY по 12, потом 25 200 по 12,1» записать нельзя — при оплате в валюте запроса курс всегда 1, а пересчёт в рубли идёт по курсу товара |
| **2.3** Окно «Создать запрос на оплату» шире + поставщик | не сделано | Ширина прежняя, поставщика в списке товаров нет |
| **5.1** Сертификат HTTPS | не сделано | Предупреждение браузера на проде остаётся; нужен домен от клиента |
| **6.5** Убрать привязку личного ТГ из интерфейса | частично | В карточке клиента остались токен и личный `chat_id` (сейчас работают как запасной канал) |

> **1.3 и 1.4 стоит показать клиенту отдельно** — это его прямое требование от 12.08 («вот тут выбора нет… и тогда ошибок не будет»), и именно оно было на скриншоте `photo_195` с прода. В текущем коде этого нет.

---

## 13. Результаты прогона 15.08.2026 (Playwright, dev-стенд)

Прогон выполнен через Playwright на `http://localhost:5173`, учётка `qa_pw` (админ). Фикстура — «золотой заказ» `QATEST-3` (№14859).

### Сводка

| Блок | Прогнано | Пройдено | Провалено | Не прогнано |
|---|---|---|---|---|
| A. Курс | A-01…A-03, A-05…A-07, A-09 | 6 | **1** (A-06) | A-04, A-08 |
| B. Шаг стрелочек | B-01…B-05 | 5 | 0 | B-06 |
| C. Реквизиты | C-01…C-03 | 3 | 0 | — |
| D. Страница «Оплаты» | D-01…D-04 | 4 | 0 | D-05, D-06 |
| E. Валюты | E-01…E-04 | 4 | 0 | — |
| F. Тема | F-01…F-04 | 4 | 0 | — |
| G. Telegram-чаты | G-01, G-02 | 2 | 0 | G-03…G-06 |
| H. Уведомления | H-01…H-03, H-08…H-11 | 7 | 0 | H-04…H-07 |
| I. Регресс | I-01 | 1 | 0 | I-02…I-05 |

**Итог: 36 кейсов прогнано, 35 пройдено, 1 провален.** Все 🔴-кейсы, кроме A-08 (нужна копия боевой БД), пройдены.

### Ключевые подтверждения

| Кейс | Ожидалось | Факт |
|---|---|---|
| A-01 | «Курс: 1 CNY = ? RUB» | ✅ так и есть |
| A-02 | 1 000 CNY ≈ 12 000 RUB | ✅ точно |
| A-03 | 100 CNY ≈ 1 200 RUB (был баг 8,33) | ✅ **исправлено** |
| A-05 | Логистика: «1 CNY = ? RUB» | ✅ **исправлено** |
| A-07 | Прибыль 13 400 RUB | ✅ доходы 20 000, закупки 4 800, логистика 600, расходы 1 200 |
| B-01…B-04 | шаги 1 / 0,1 / 0,01 | ✅ `0,000001` больше не появляется |
| B-05 | 1 234,56 и курс 12,417 сохраняются | ✅ явный `step` ввод не блокирует |
| C-02 | длинная китайская строка переносится | ✅ горизонтального переполнения нет |
| D-01…D-04 | фильтр `>0`, отменённые скрыты | ✅ 4↔11 заявок, отменённый заказ исчез при обоих фильтрах, внутри заказа остался |
| E-02 | легаси-запись в EUR читается | ✅ 100 EUR × 95 = 9 500 RUB, ошибок нет |
| E-03 | API отклоняет EUR | ✅ 422 «Input should be 'USD', 'CNY' or 'RUB'» |
| F-01 | сайдбар серый, темнее фона | ✅ `#e3e7ee` против `#eef1f5` |
| H-01…H-03 | шаблоны с хештегом | ✅ совпали символ в символ, «Остаток после оплаты: 600 CNY» считается после платежа |
| H-08 | сбой уведомления не роняет операцию | ✅ 3 операции сохранены (201) при недоставленных уведомлениях |
| I-01 | 317 passed | ✅ на базе `ibra_test` |

---

## 14. 🐞 БАГ-1: курс ≠ 1 принимается для записи в валюте заказа

**Severity: высокая. Найден при прогоне A-06, подтверждён на API.**

### Суть

Если валюта записи совпадает с валютой заказа, курс обязан быть `1`. Для **товаров** это проверяется, для **доходов/расходов, приёмки логистики и оплат — нет**. Любое число проходит и молча умножает сумму.

### Воспроизведение (UI)

1. Заказ в **RUB** → вкладка ДиР → «Добавить доход».
2. Сумма **20 000**, валюта **RUB** (= валюте заказа).
3. Поле «Курс (вручную)» остаётся видимым и редактируемым — вписать **5**.
4. Сохранить, открыть заказ.

**Ожидается:** либо поле курса скрыто (как в форме товара), либо отказ 422.
**Фактически:** запись сохраняется, в блоке прибыли **«Доходы: 100 000 RUB»** вместо 20 000, прибыль раздувается до 93 400 RUB (67% → 93,4%).

### Подтверждение на API

| Запрос | Тело | Ответ |
|---|---|---|
| `POST /api/orders/{id}/products/` | `currency: RUB, exchange_rate: 7` | **422** «Exchange rate must be 1 when the product is priced in the order's currency (RUB)» |
| `POST /api/orders/{id}/ledger-entries/` | `currency: RUB, exchange_rate: 7` | **201 Created** ❌ |
| `POST /api/orders/{id}/payment-requests/{r}/payments/` | `currency: CNY` (= валюте запроса)`, exchange_rate: 5` | **201 Created** ❌ |

### Причина

Проверка живёт в единственном месте — [backend/app/routers/products.py](backend/app/routers/products.py) в `_resolve_exchange_rate()`. Её комментарий прямо описывает риск: *принять что-то кроме 1 значило бы незаметно исказить расчёт прибыли*. В [ledger_entries.py](backend/app/routers/ledger_entries.py), [logistics.py](backend/app/routers/logistics.py) (`accept_logistics`) и [payments.py](backend/app/routers/payments.py) курс кладётся в модель как есть, схемы ограничивают только `gt=0`.

### Почему не поймали автотесты

Guard не покрыт ни одним тестом: `grep "must be 1" backend/tests/` не находит ничего. Поэтому 317 зелёных тестов ничего не говорят об этом правиле.

### Что чинить

1. Вынести `_resolve_exchange_rate()` из `products.py` в общий сервис и применить в `ledger_entries`, `logistics.accept`, `payments` (для оплат эталон — валюта запроса).
2. Во фронтенде скрывать поле курса при совпадении валют — как это делает [CurrencyRateFields.tsx](frontend/src/components/CurrencyRateFields.tsx). Сейчас `CreateLedgerEntryModal` и `LogisticsDetailPanel` показывают «Курс (вручную)» со значением 1.
3. Добавить тесты на все четыре пути.

### Связь с требованием клиента

Прямо противоречит его формулировке от 13.08, 13:48:
> «если валюта заказа рубль, и мы для записи выбрали рубль, то система сама понимает, что курс должен быть 1 к 1»

---

## 15. ⚪ Мелочи (не блокеры)

1. **Курс в таблице ДиР** выводится как `1 CNY = 12.000000 RUB` — с хвостом нулей, тогда как в уведомлениях он форматируется как `12`. Стоит переиспользовать форматирование из [notification_templates.py](backend/app/services/notification_templates.py).
2. **Несогласованность подписи поля курса:** в форме товара и логистики — «Курс: 1 CNY = ? RUB», в форме оплаты по заявке — «Курс (вручную)» без указания направления.

---

## 16. Что не прогнано и почему

| Кейсы | Причина |
|---|---|
| A-08 (миграция на старых данных) | Нужна копия боевой БД. На dev курсы и так уже в нужном направлении: `ibra_orders` никогда не проходила инверсию от 08.08, см. §0 |
| H-04…H-07 (реальная доставка, файлы, несколько чатов, fallback) | Требует отправки сообщений в **живые** Telegram-чаты («ibra new system 2», «Ibra New System Test»). Не запускал без явного разрешения — это внешнее действие с реальными получателями. Текст шаблонов проверен по логам |
| G-03…G-06 | Привязка/отвязка приводит к реальной доставке при следующей операции — та же причина |
| D-05, I-04 (роли) | Нужны пароли к учёткам `manager` / `observer` |
| I-02 (`npm run build`) | Не запускался |
| B-06 | Визуальная проверка наличия стрелок; атрибуты `step` подтверждены программно во всех формах |
