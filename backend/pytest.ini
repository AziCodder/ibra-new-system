[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    key_access: key logic — role-based access control
    key_profit: key logic — profit calculation (ТЗ §11)
    key_balance: key logic — payment/shipment remaining balance
    key_numbering: key logic — order auto-numbering
