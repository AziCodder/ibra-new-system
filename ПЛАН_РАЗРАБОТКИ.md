# План разработки — Система внутреннего учёта заказов

> Детальный пошаговый чек-лист. Каждый шаг маленький и самодостаточный: **Задача** (что делаем) · **Цель** (зачем) · **Итог** (как понять, что готово — проверяемо).
> Отмечай прогресс галочками `[x]`. Шаги внутри фазы идут по порядку; фазы в целом тоже последовательны, но 11 (Telegram), 13 (адаптив) и 14 (аналитика-схема) можно подмешивать раньше.

## Технологический стек (зафиксировано)

| Слой | Выбор | Почему |
|------|-------|--------|
| Бэкенд | **FastAPI** (Python 3.12, async) | Быстрый, типизированный, авто-доки (OpenAPI) |
| ORM/миграции | **SQLAlchemy 2.0 (async) + Alembic** | Стабильность, контроль схемы |
| Валидация | **Pydantic v2** | Строгие схемы вход/выход |
| БД | **PostgreSQL 16** | Реляционные связи, надёжность |
| Аутентификация | **Сессии в httpOnly-cookie** | Безопаснее JWT в localStorage для дашборда |
| Пароли | **argon2 (passlib)** | Современное хеширование |
| Файлы | **Модуль хранилища** (локальный диск, интерфейс под S3/MinIO) | Гибкость, переезд без переписывания |
| Telegram | **aiogram** (async Bot), фоновая отправка | Чистая структура, легко расширять; сейчас только исходящие уведомления в группы |
| Фронтенд | **React + Vite + TypeScript** | Скорость, типобезопасность |
| Стили/адаптив | **Tailwind CSS** | Адаптив из коробки, быстрый UI |
| Данные на фронте | **TanStack Query** | Кеш, ретраи, стабильный UX |
| Роутинг | **React Router** | Стандарт |

**Принципы:** адаптивность (ПК + телефон), скорость, стабильность. Каждая операция, меняющая данные → **проверка прав на бэкенде** (не только в UI). Все денежные операции хранят свою валюту и **введённый вручную курс** к валюте заказа.

**Прототип** (`prototype/`) — визуальный референс, переносим оформление в компоненты. **`remotion-test/`** — отдельный эксперимент, к системе отношения не имеет.

---

## Фаза 0 — Фундамент и окружение

- [x] **0.1 — Структура репозитория**
  - Задача: создать `backend/`, `frontend/`, `docker-compose.yml`, `.gitignore`, `README.md`. `git init`.
  - Цель: единый каркас, в котором всё дальше живёт.
  - Итог: дерево папок на месте, репозиторий инициализирован, первый коммит.
- [x] **0.2 — PostgreSQL в Docker**
  - Задача: сервис `db` в docker-compose (Postgres 16, volume, порт, креды из `.env`).
  - Цель: воспроизводимая БД локально.
  - Итог: `docker compose up db` поднимает базу, подключение через psql/клиент работает.
- [x] **0.3 — Скелет FastAPI**
  - Задача: `main.py` с приложением и эндпоинтом `GET /health` → `{"status":"ok"}`. `requirements.txt`/`pyproject`.
  - Цель: рабочий бэкенд-процесс.
  - Итог: `uvicorn` запускается, `/health` отвечает, `/docs` открывается.
- [x] **0.4 — Конфиг через pydantic-settings**
  - Задача: `Settings` (URL БД, секрет сессий, путь файлов, токен бота) из `.env`.
  - Цель: никаких хардкодов секретов.
  - Итог: приложение читает настройки из `.env`, есть `.env.example`.
- [x] **0.5 — Подключение SQLAlchemy + Alembic**
  - Задача: async-движок, `Base`, фабрика сессий, инициализация Alembic.
  - Цель: фундамент для моделей и миграций.
  - Итог: `alembic revision --autogenerate` и `alembic upgrade head` отрабатывают на пустой схеме.
- [x] **0.6 — Скелет фронтенда**
  - Задача: Vite + React + TS + Tailwind + React Router + TanStack Query. Запрос к `/health`, вывод статуса.
  - Цель: рабочий фронтенд-процесс, связь с API.
  - Итог: `npm run dev` открывает страницу, видно «API: ok».
- [x] **0.7 — CORS и dev-прокси**
  - Задача: настроить CORS на бэке и прокси Vite на `/api`.
  - Цель: фронт и бэк общаются без ошибок в разработке.
  - Итог: запрос с фронта на `/api/health` проходит без CORS-ошибок.
- [x] **0.8 — Линтеры и форматтеры**
  - Задача: ruff + mypy (backend), eslint + prettier (frontend).
  - Цель: стабильность и единый стиль с самого старта.
  - Итог: `ruff check`, `mypy`, `eslint` проходят на чистом проекте.
- [x] **0.9 — Docker-compose dev-окружение**
  - Задача: сервисы `db`, `backend`, `frontend` одной командой.
  - Цель: запуск всего проекта в один шаг.
  - Итог: `docker compose up` поднимает все три, приложение открывается в браузере.

---

## Фаза 1 — Аутентификация и роли

- [x] **1.1 — Модель User**
  - Задача: модель `User` (id, логин, хеш пароля, роль `admin|manager|observer`, ФИО, активен, created_at) + миграция.
  - Цель: основа доступа.
  - Итог: таблица `users` создана миграцией.
- [x] **1.2 — Хеширование паролей**
  - Задача: утилиты `hash_password` / `verify_password` (argon2).
  - Цель: безопасное хранение паролей.
  - Итог: юнит-тест: верный пароль проходит, неверный — нет.
- [x] **1.3 — Сидинг первого админа (CLI)**
  - Задача: скрипт `create_admin` (логин+пароль из аргументов/ввода).
  - Цель: есть кем войти в пустую систему.
  - Итог: команда создаёт админа, запись видна в БД.
- [x] **1.4 — Логин и сессия**
  - Задача: `POST /api/auth/login` (проверка пароля → httpOnly-cookie сессии), `POST /api/auth/logout`.
  - Цель: вход/выход.
  - Итог: верные креды → 200 + cookie; неверные → 401; logout чистит сессию.
- [x] **1.5 — Текущий пользователь**
  - Задача: dependency `get_current_user` (из cookie), `GET /api/auth/me`.
  - Цель: знать, кто делает запрос.
  - Итог: `/me` возвращает пользователя при валидной сессии, 401 без неё.
- [x] **1.6 — Проверка ролей на бэкенде**
  - Задача: dependency `require_role(*roles)` для эндпоинтов.
  - Цель: backend-проверка прав на каждой операции (ключевое требование ТЗ).
  - Итог: эндпоинт под `require_role("admin")` отдаёт 403 менеджеру.
- [x] **1.7 — Фронт: страница логина**
  - Задача: форма входа, хранение сессии (cookie), запрос `/me` при загрузке.
  - Цель: точка входа в приложение.
  - Итог: ввод верных кредов пускает внутрь, неверных — показывает ошибку.
- [x] **1.8 — Фронт: защита маршрутов**
  - Задача: guard-обёртка, редирект неавторизованных на `/login`, скрытие пунктов меню по роли.
  - Цель: UI соответствует правам.
  - Итог: без входа любой внутренний маршрут редиректит на логин; наблюдатель не видит кнопок изменения.
- [x] **1.9 — Управление пользователями (только админ)**
  - Задача: CRUD пользователей API + экран (создать логин/пароль, сменить роль, деактивировать). Передача аккаунта уволенного = смена ФИО/пароля.
  - Цель: админ заводит сотрудников.
  - Итог: админ создаёт менеджера, тот может войти; не-админ к разделу не имеет доступа (403).

---

## Фаза 2 — Справочники (раздел «База данных»)

- [x] **2.1 — Модель Клиент**
  - Задача: `Client` (id, уникальный код напр. `M33`, ФИО, описание, ссылка ТГ-группы) + миграция.
  - Цель: ключевая сущность для заказов.
  - Итог: таблица `clients`, уникальность кода на уровне БД.
- [x] **2.2 — CRUD клиентов**
  - Задача: API список/создать/изменить/удалить + права (изменяет админ).
  - Цель: вести клиентов.
  - Итог: создание дубля кода → 409; удаление клиента с заказами запрещено.
- [x] **2.3 — Модель Поставщик**
  - Задача: `Supplier` (id, название, контакты/детали) + миграция.
  - Цель: нужен при добавлении товара.
  - Итог: таблица `suppliers` создана.
- [x] **2.4 — CRUD поставщиков**
  - Задача: API список/создать/изменить/удалить.
  - Цель: вести поставщиков.
  - Итог: CRUD работает, проверен через `/docs`.
- [x] **2.5 — Фронт: раздел «База данных»**
  - Задача: страница с вкладками Пользователи / Клиенты / Поставщики, таблицы + формы.
  - Цель: единое место управления справочниками.
  - Итог: на каждой вкладке видно список и можно добавить/изменить запись.

---

## Фаза 3 — Общий модуль файлов

- [x] **3.1 — Абстракция хранилища**
  - Задача: интерфейс `Storage.save/get/delete` + реализация на локальном диске, лимиты размера/типа.
  - Цель: единый способ работы с файлами для всех сущностей.
  - Итог: юнит-тест: файл сохраняется и читается, превышение лимита отклоняется.
- [x] **3.2 — Загрузка/выдача файлов API**
  - Задача: эндпоинты загрузки (multipart) и безопасной отдачи (проверка прав на скачивание).
  - Цель: переиспользуемый файловый слой.
  - Итог: файл грузится, скачивается по защищённой ссылке; чужой без прав → 403.
- [x] **3.3 — Фронт: компонент загрузки файлов**
  - Задача: переиспользуемый аплоадер (drag&drop, превью, лимиты, удаление).
  - Цель: единый UI для файлов заказа/товара/запроса/логистики/оплаты.
  - Итог: компонент грузит файл, показывает прогресс и список, удаляет.

---

## Фаза 4 — Заказы: каркас

- [x] **4.1 — Модель Заказ**
  - Задача: `Order` (id, номер, клиент, менеджер, дата, статус `в работе|завершён|отменён`, детали, валюта заказа) + миграция.
  - Цель: центральная сущность системы.
  - Итог: таблица `orders` с FK на клиента и менеджера.
- [x] **4.2 — Автогенерация номера заказа**
  - Задача: сервис номера `{код клиента}-{порядковый}` (`M33-1`, `M33-2`).
  - Цель: уникальные читаемые номера.
  - Итог: тест: два заказа одного клиента получают `-1` и `-2` без коллизий (конкурентно безопасно).
- [x] **4.3 — API создания и чтения заказа**
  - Задача: `POST /api/orders`, `GET /api/orders/{id}`. Менеджер по умолчанию = текущая сессия.
  - Цель: завести и открыть заказ.
  - Итог: заказ создаётся, возвращается с номером; чтение по id работает.
- [x] **4.4 — Список заказов: фильтры + пагинация**
  - Задача: `GET /api/orders` с фильтрами (клиент/статус/менеджер) и пагинацией на бэке.
  - Цель: рабочий список главной страницы.
  - Итог: фильтры и страницы возвращают корректные срезы.
- [x] **4.5 — Права видимости заказов**
  - Задача: админ/наблюдатель — все; менеджер — только свои; фильтр клиентов у менеджера ограничен.
  - Цель: разграничение доступа по ТЗ.
  - Итог: менеджер не видит чужие заказы (и в списке, и по прямому id → 403/404).
- [x] **4.6 — Фронт: список заказов плиткой**
  - Задача: перенести вид плитки из прототипа на реальные данные + фильтры + пагинация.
  - Цель: главный рабочий экран.
  - Итог: карточки заказов отражают данные из API, фильтры применяются.
- [x] **4.7 — Фронт: создание заказа**
  - Задача: форма (детали, клиент, менеджер по умолчанию — текущий, дата) → переход на страницу заказа.
  - Цель: завести заказ из UI.
  - Итог: после создания открывается страница нового заказа.
- [x] **4.8 — Фронт: страница заказа (каркас 4 окон)**
  - Задача: шапка заказа + переключаемые вкладки **Товары → Запросы на оплату → Логистика → ДиР** (пока пустые).
  - Цель: контейнер для всей работы по заказу.
  - Итог: вкладки переключаются, шапка показывает номер/клиента/статус.
- [x] **4.9 — Редактирование и удаление заказа**
  - Задача: правка (детали — только админ), удаление только если нет товаров/оплат/запросов/доходов/расходов/логистик.
  - Цель: безопасное управление заказом.
  - Итог: удаление заказа со связями → 409 с пояснением; пустой удаляется.
- [x] **4.10 — Заметки заказа**
  - Задача: модель `Note` (текст, автор, дата) + API + блок внизу страницы.
  - Цель: история обсуждения заказа.
  - Итог: заметка сохраняется с автором и датой, видна в ленте.

---

## Фаза 5 — Окно 1: Товары

- [x] **5.1 — Модель Товар**
  - Задача: `Product` (заказ, наименование, детали, кол-во, цена, валюта, поставщик, фото) + миграция.
  - Цель: позиции заказа.
  - Итог: таблица `products` с FK на заказ и поставщика.
- [x] **5.2 — CRUD товаров в заказе**
  - Задача: API добавить/изменить/удалить товар; 1 фото (через модуль файлов).
  - Цель: вести список товаров.
  - Итог: товар создаётся с фото, изменяется, удаляется.
- [x] **5.3 — Запрет удаления связанного товара**
  - Задача: блок удаления, если есть ≥1 запрос на оплату или логистика по товару.
  - Цель: целостность данных.
  - Итог: попытка удалить связанный товар → 409.
- [x] **5.4 — Фронт: таблица-превью товаров**
  - Задача: колонки (наименование, кол-во, цена+валюта, сумма), итоги внизу (кол-во + общая сумма).
  - Цель: обзор товаров заказа.
  - Итог: таблица и итоги считаются корректно.
- [x] **5.5 — Фронт: форма добавления товара**
  - Задача: форма с полями + загрузка фото.
  - Цель: добавлять товары из UI.
  - Итог: новый товар появляется в таблице.
- [x] **5.6 — Фронт: детальная карточка товара**
  - Задача: боковая панель: детали, поставщик, сумма счетов/оплачено/остаток, отправлено/трекинги.
  - Цель: полная картина по позиции.
  - Итог: по клику открывается карточка со сводкой остатков.

---

## Фаза 6 — Окно 2: Запросы на оплату (закупки)

- [x] **6.1 — Модель Запрос на оплату**
  - Задача: `PaymentRequest` (заказ, позиции=товар+сумма, реквизиты, детали, приоритет, файлы до 3×5МБ) + миграция.
  - Цель: оформление закупок.
  - Итог: таблицы запроса и его позиций созданы.
- [x] **6.2 — Контроль остатков и валюты**
  - Задача: суммы не превышают остаток по товару; запрет товаров с разной валютой в одном запросе.
  - Цель: корректность закупок.
  - Итог: превышение/смешение валют → 422 с понятной ошибкой.
- [x] **6.3 — CRUD запроса**
  - Задача: API создать/изменить/удалить; удаление запрещено, если есть ≥1 оплата.
  - Цель: управление запросами.
  - Итог: запрос с оплатой не удаляется (409).
- [x] **6.4 — Модель и внесение оплаты (частичная)**
  - Задача: `Payment` (запрос, сумма, валюта, курс вручную, файл, примечание, автор, дата); отслеживание остатка.
  - Цель: фиксация оплат с остатком.
  - Итог: несколько частичных оплат корректно уменьшают остаток.
- [x] **6.5 — Хук уведомления (заглушка)**
  - Задача: точка вызова уведомления при создании запроса (реальная отправка — Фаза 11).
  - Цель: подготовить интеграцию ТГ.
  - Итог: при создании запроса вызывается `notify()` (пока лог).
- [x] **6.6 — Фронт: создание запроса**
  - Задача: окно со всеми товарами заказа, выбор галочками (авто-сумма = остаток, редактируется), итог снизу, реквизиты/детали/приоритет, файлы.
  - Цель: оформить закупку из UI.
  - Итог: запрос создаётся с выбранными позициями и суммами.
- [x] **6.7 — Фронт: карточка запроса и оплаты**
  - Задача: детальная карточка (товары, остаток, «Копировать информацию»), форма «Внести оплату».
  - Цель: вести оплаты по запросу.
  - Итог: оплата вносится, остаток пересчитывается на экране.
- [x] **6.8 — Фронт: превью запроса в списке окна**
  - Задача: полная сумма, остаток в родной валюте, оплачено в валюте заказа по курсам оплат.
  - Цель: быстрый обзор закупок.
  - Итог: цифры превью совпадают с детальной карточкой.

---

## Фаза 7 — Окно 3: Логистика

- [x] **7.1 — Модель Логистика**
  - Задача: `Logistics` (заказ, товар, кол-во, трекинг, дата отправки, накладная, статус `в дороге|принят|отменён`, дата получения, расход, валюта, курс) + миграция.
  - Цель: учёт отправок.
  - Итог: таблица `logistics` создана.
- [x] **7.2 — Контроль остатка отправки**
  - Задача: нельзя отправить больше, чем закуплено минус уже отправлено.
  - Цель: не отправить лишнее.
  - Итог: тест: закуплено 20, отправлено 10 → доступно 10, на 11 → ошибка.
- [x] **7.3 — CRUD и права логистики**
  - Задача: API создать/изменить/удалить; менеджер правит/удаляет только «в дороге», админ — шире.
  - Цель: безопасное ведение логистики.
  - Итог: менеджер не может изменить принятую логистику (403).
- [x] **7.4 — Приёмка и распринятие**
  - Задача: «Принять» (только админ): дата/сумма/валюта/курс → статус «принят», данные заморожены; «Распринять» → откат в «в дороге».
  - Цель: фиксация прихода и возможность отката.
  - Итог: приёмка замораживает запись; распринятие возвращает редактируемость.
- [x] **7.5 — Комментарии логистики**
  - Задача: модель `LogisticsComment` (автор, дата); добавлять могут все, на любом статусе.
  - Цель: обсуждение поставки.
  - Итог: комментарий добавляется даже к отменённой/принятой логистике.
- [x] **7.6 — Хук уведомления о получении**
  - Задача: точка вызова уведомления «товар получен» (отправка — Фаза 11).
  - Цель: подготовить ТГ-уведомление.
  - Итог: кнопка вызывает `notify()` (пока лог).
- [x] **7.7 — Фронт: окно логистики в заказе**
  - Задача: список логистик заказа, форма создания (товар, кол-во, трекинг, дата, накладная), детальная карточка с кнопками (редактировать/удалить/копировать/принять/распринять/уведомить).
  - Цель: вести логистику из UI.
  - Итог: полный цикл логистики проходится из интерфейса.

---

## Фаза 8 — Окно 4: ДиР (Доходы и Расходы)

- [ ] **8.1 — Модель записи ДиР**
  - Задача: `LedgerEntry` (заказ, тип `доход|расход`, сумма, валюта, курс, детали, автор, дата) + миграция.
  - Цель: учёт прочих доходов/расходов.
  - Итог: таблица создана, тип фиксируется при создании.
- [ ] **8.2 — API ДиР**
  - Задача: добавить доход/расход, удалить (редактирование запрещено).
  - Цель: вести ДиР.
  - Итог: запись добавляется и удаляется; попытка изменить → 405/403.
- [ ] **8.3 — Фронт: окно ДиР**
  - Задача: две кнопки (доход/расход), общая таблица с колонкой типа, раздельные итоги, карточка с автором.
  - Цель: управлять ДиР из UI.
  - Итог: итоги по доходам и расходам считаются раздельно и верно.

---

## Фаза 9 — Расчёт прибыли и итоги

- [ ] **9.1 — Сервис расчёта прибыли**
  - Задача: привести все операции к валюте заказа по их курсам; формула `Доходы − Закупки − Логистика − Прочие расходы`.
  - Цель: итоговая прибыль заказа.
  - Итог: юнит-тесты на оба примера из ТЗ (рубль и доллар) дают ожидаемые числа.
- [ ] **9.2 — Условие готовности расчёта**
  - Задача: итоги считаются только когда весь товар отправлен И все логистики приняты.
  - Цель: не считать незавершённые заказы.
  - Итог: до выполнения условия итог помечен «не готов», после — считается.
- [ ] **9.3 — Фиксация метрик заказа**
  - Задача: при готовности сохранить процент прибыли и кол-во дней обработки.
  - Цель: данные для будущей аналитики и завершения.
  - Итог: метрики сохраняются и видны на странице заказа.
- [ ] **9.4 — Фронт: блок итогов**
  - Задача: блок «Прибыль» на странице заказа (разбивка по составляющим, %, дни).
  - Цель: наглядный финансовый итог.
  - Итог: блок показывает расчёт или «ожидает завершения логистики».
- [ ] **9.5 — Статусы заказа и переходы**
  - Задача: правила статусов (в работе/завершён/отменён) по §13: кто меняет, заморозка, возврат.
  - Цель: корректный жизненный цикл заказа.
  - Итог: «Завершён» доступен только при оплате+приёмке всего; переходы по правам.

---

## Фаза 10 — Сводные разделы

- [ ] **10.1 — Сводка «Запросы на оплату»**
  - Задача: один экран со всеми запросами; фильтры (работник/клиент), сортировка по дате; колонки с номером заказа-ссылкой; внесение оплат.
  - Цель: чтобы оплаты не терялись.
  - Итог: сводка собирает запросы из всех заказов, оплата вносится прямо отсюда.
- [ ] **10.2 — Сводка «Логистика» (одна страница)**
  - Задача: все логистики на одном экране; фильтры (статус/клиент/менеджер) + поиск; колонки (трекинг, номер заказа-ссылка, клиент, менеджер, даты, расход); действия в боковой панели.
  - Цель: коллега в Москве видит весь товар в пути не прыгая по заказам.
  - Итог: фильтры/поиск работают, действия (принять/редактировать/копировать) доступны из сводки.

---

## Фаза 11 — Telegram-интеграция

- [ ] **11.1 — Конфиг бота и привязка групп**
  - Задача: токен бота в настройках; единый async-экземпляр `aiogram.Bot` (lifespan FastAPI, корректное закрытие сессии); у клиента — `chat_id`/ссылка ТГ-группы.
  - Цель: знать, куда слать уведомления, и иметь готовый бот.
  - Итог: тестовое сообщение через aiogram уходит в группу клиента; сессия бота закрывается при остановке приложения.
- [ ] **11.2 — Модуль отправки**
  - Задача: обёртка `notify(chat_id, text)` поверх `bot.send_message`, в фоне (BackgroundTasks), с ретраями. Поллинг/диспетчер НЕ запускаем (только исходящие).
  - Цель: надёжная доставка.
  - Итог: сообщение отправляется асинхронно, не блокируя ответ API.
- [ ] **11.3 — Уведомление о запросе на оплату**
  - Задача: подключить к хуку 6.5; формат «по заказу XXX выставлен запрос на сумму, реквизиты, ссылка».
  - Цель: клиент сразу видит счёт.
  - Итог: создание запроса присылает корректное сообщение в группу.
- [ ] **11.4 — Уведомление о получении товара**
  - Задача: подключить к хуку 7.6 (кнопка в логистике).
  - Цель: уведомить о приходе груза.
  - Итог: нажатие кнопки шлёт уведомление в группу.
- [ ] **11.5 — Обработка ошибок отправки**
  - Задача: ловить недоступность группы, логировать, не падать; индикатор «не доставлено».
  - Цель: стабильность при проблемах с ТГ.
  - Итог: при ошибке отправки система работает, ошибка залогирована и видна.

---

## Фаза 12 — Аудит и история

- [ ] **12.1 — Сквозной аудит автор+дата**
  - Задача: убедиться, что заметки, доходы/расходы, комментарии логистики, платежи фиксируют автора и дату; свести в единый паттерн.
  - Цель: прозрачность изменений.
  - Итог: у каждой такой записи виден автор и дата.
- [ ] **12.2 — (Опционально) Журнал действий**
  - Задача: лог ключевых операций (создание/изменение статуса/приёмка).
  - Цель: расследование спорных ситуаций.
  - Итог: журнал доступен админу (если решим делать).

---

## Фаза 13 — Роль «Наблюдатель», адаптив и UX-полировка

- [ ] **13.1 — Наблюдатель: только чтение + комментарии**
  - Задача: на бэке и фронте запретить наблюдателю любые изменения, кроме комментариев.
  - Цель: соответствие ТЗ по роли.
  - Итог: наблюдатель видит всё, кнопок изменений нет, комментарий оставить может.
- [ ] **13.2 — Мобильный адаптив**
  - Задача: проверить все экраны на телефоне; таблицы → карточки, выезжающий сайдбар, удобные формы.
  - Цель: «максимально хорошо адаптивный» (требование).
  - Итог: ключевые сценарии проходятся на ширине ~375px без горизонтального скролла.
- [ ] **13.3 — Состояния загрузки/ошибок/пусто**
  - Задача: скелетоны, тосты ошибок, пустые состояния, оптимистичные обновления где уместно (TanStack Query).
  - Цель: стабильный и быстрый UX.
  - Итог: ни один экран не «моргает» белым; ошибки понятны пользователю.

---

## Фаза 14 — Схема под будущую аналитику (без UI)

- [ ] **14.1 — Поля и индексы под метрики**
  - Задача: проверить, что есть даты закрытия, прибыльность, объёмы; добавить индексы для будущих отчётов.
  - Цель: добавить аналитику позже без переделок (требование ТЗ).
  - Итог: схема содержит нужные поля/индексы; UI аналитики НЕ делаем.

---

## Фаза 15 — Стабильность, тесты, деплой

- [ ] **15.1 — Тесты ключевой логики**
  - Задача: pytest на права доступа, расчёт прибыли, контроль остатков (оплата/отправка), автонумерацию.
  - Цель: защита от регрессий.
  - Итог: тесты зелёные в CI.
- [ ] **15.2 — Прод-конфигурация**
  - Задача: prod-переменные, HTTPS, безопасные cookie, CORS-ограничения, бэкап БД.
  - Цель: безопасный прод.
  - Итог: чек-лист безопасности пройден.
- [ ] **15.3 — Деплой**
  - Задача: прод-сборка фронта, Docker-образы, запуск на сервере, миграции при старте.
  - Цель: рабочая система в проде.
  - Итог: приложение доступно по адресу, вход и базовый сценарий проходят.
- [ ] **15.4 — Логи и мониторинг**
  - Задача: структурные логи, ошибки в один поток, базовый аптайм-чек.
  - Цель: видеть проблемы в проде.
  - Итог: ошибки фиксируются и доступны для просмотра.

---

### Как пользоваться планом
1. Берём один шаг, делаем его целиком до «Итога».
2. Отмечаем `[x]`, после правок кода — `graphify update .` для актуализации графа.
3. Не начинаем следующий шаг, пока «Итог» текущего не выполнен и не проверен.
