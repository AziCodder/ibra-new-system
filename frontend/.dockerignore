# Хостовые node_modules (200+ МБ) в образ не едут: они собраны под Windows,
# а внутри нужен linux-вариант. Заодно build context перестаёт весить сотни
# мегабайт на каждой сборке.
node_modules
dist
.env
.env.*
