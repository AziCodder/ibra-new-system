# Progress — Ibra Order System

## Текущая фаза: 10
## Текущая задача: 10.2 — Сводный экран логистики

## Выполненные задачи

| Задача | Описание | Дата | Статус |
|--------|----------|------|--------|
| 0.1 | Структура репозитория (backend/, frontend/, docker-compose, .gitignore, README, docs) | 2026-06-29 19:45 | ✅ |
| 0.2 | PostgreSQL в Docker (healthcheck, pg_isready + psql подключение проверено) | 2026-06-29 19:51 | ✅ |
| 0.3 | Скелет FastAPI (main.py, /health, /docs, requirements.txt) | 2026-06-29 19:57 | ✅ |
| 0.4 | Конфиг через pydantic-settings (Settings из .env, проверено) | 2026-06-29 20:03 | ✅ |
| 0.5 | SQLAlchemy + Alembic (async engine, Base, sync psycopg миграции, port 5433) | 2026-06-29 20:11 | ✅ |
| 0.6 | Скелет фронтенда (Vite+React+TS+Tailwind+Router+Query, dark/light токены, health check) | 2026-06-29 20:19 | ✅ |
| 0.7 | CORS и dev-прокси (FastAPI CORSMiddleware + Vite proxy) | 2026-06-29 20:24 | ✅ |
| 0.8 | Линтеры (ruff backend, eslint+typescript-eslint frontend, оба чистые) | 2026-06-29 20:30 | ✅ |
| 0.9 | Docker-compose dev-окружение (db + backend + frontend, hot-reload) | 2026-06-29 21:15 | ✅ |
| 1.1 | Модель User (id, login, password_hash, role, full_name, is_active, created_at) + миграция | 2026-06-29 21:20 | ✅ |
| 1.2 | Хеширование паролей argon2 (hash_password/verify_password + 3 теста) | 2026-06-29 21:25 | ✅ |
| 1.3 | Сидинг первого админа (CLI: python -m app.scripts.create_admin) | 2026-06-29 21:30 | ✅ |
| 1.4 | Логин/логаут с httpOnly cookie сессией (itsdangerous) | 2026-06-29 21:35 | ✅ |
| 1.5 | GET /api/auth/me — текущий пользователь | 2026-06-29 21:40 | ✅ |
| 1.6 | require_role dependency для проверки ролей на бэкенде | 2026-06-29 21:45 | ✅ |
| 1.7 | Фронт: страница логина (AuthContext, LoginPage, RequireAuth) | 2026-06-29 21:55 | ✅ |
| 1.8 | Фронт: защита маршрутов (RequireAuth + redirect to /login) | 2026-06-29 21:55 | ✅ |
| 1.9 | Управление пользователями CRUD (только админ, backend+frontend) | 2026-06-29 22:05 | ✅ |
| 2.1 | Модель Client (code, full_name, description, tg_link) + миграция | 2026-06-29 22:15 | ✅ |
| 2.2 | CRUD клиентов API (list/create/update/delete, admin only) | 2026-06-29 22:20 | ✅ |
| 2.3 | Модель Supplier (name, contacts, details) + миграция | 2026-06-29 22:25 | ✅ |
| 2.4 | CRUD поставщиков API | 2026-06-29 22:25 | ✅ |
| 2.5 | Фронт: раздел «База данных» (вкладки Users/Clients/Suppliers) | 2026-06-29 22:30 | ✅ |
| 3.1 | Абстракция хранилища файлов (LocalStorage + 4 теста) | 2026-06-29 22:35 | ✅ |
| 3.2 | Загрузка/выдача файлов API (upload/download/delete) | 2026-06-29 22:40 | ✅ |
| 3.3 | Фронт: компонент загрузки файлов (drag&drop, прогресс, список) | 2026-06-29 22:45 | ✅ |
| 4.1 | Модель Order (number, client_id, manager_id, status, currency) + миграция, проверено в БД | 2026-06-30 09:10 | ✅ |
| 4.2 | Автогенерация номера заказа (FOR UPDATE lock, тест на конкурентность) | 2026-06-30 09:20 | ✅ |
| 4.3 | API создания/чтения заказа (POST/GET), проверено end-to-end | 2026-06-30 09:30 | ✅ |
| 4.4 | Список заказов: фильтры (client/status/manager) + пагинация, проверено | 2026-06-30 09:45 | ✅ |
| 4.5 | Права видимости заказов (manager видит только свои), проверено | 2026-06-30 10:00 | ✅ |
| 4.6 | Фронт: AppShell (sidebar) + список заказов плиткой, фильтры, пагинация. Проверено в браузере — нашёл и исправил 2 бага: vite-proxy /api rewrite, LoginPage без редиректа | 2026-06-30 10:30 | ✅ |
| 4.7 | Фронт: модалка создания заказа + переход на страницу заказа (OrderDetailPage-плейсхолдер). Проверено в браузере end-to-end после восстановления Docker: модалка открывается, клиент/валюта/детали заполняются, менеджер показан read-only, сохранение создаёт заказ и переходит на /orders/:id, страница заказа отображает корректные данные, "← Назад" возвращает в список с новой плиткой | 2026-06-30 12:05 | ✅ |
| 4.8 | Фронт: каркас страницы заказа — 4 вкладки (Товары/Запросы на оплату/Логистика/ДиР), переключение с индиго-подчёркиванием, пустые плейсхолдеры. tsc+eslint чисто, паттерн идентичен уже проверенным (4.6 TabBar, 4.7 шапка). Docker не поднялся 2 тика подряд — закрываю по статической проверке; полная визуальная сверка по плану будет в фазе HARDEN (§7 инструкции) | 2026-06-30 12:05 | ✅ |
| 4.9 | PATCH/DELETE /api/orders/{id} (admin only) + count_order_dependencies() + 3 теста. Docker снова поднялся — запустил pytest: 3 теста падали с asyncpg `InterfaceError`/`Event loop is closed` при прогоне всего набора (module-level engine делил connection pool между тестами на разных event loop в pytest-asyncio strict mode на Windows ProactorEventLoop). Исправил добавлением `tests/conftest.py` с autouse `@pytest_asyncio.fixture`, который вызывает `engine.dispose()` после каждого теста — это инфраструктурный фикс тестового набора, не относится к бизнес-логике 4.9. Полный набор: 11 passed | 2026-06-30 16:50 | ✅ |
| 4.10 | Модель `Note` (order_id FK CASCADE, author_id FK, text, created_at) + миграция + API `/api/orders/{id}/notes` (GET список хронологически, POST с правами видимости заказа — manager видит/добавляет заметки только на своих заказах, 404 иначе) + фронт `NotesSection` (блок внизу страницы заказа под вкладками, не отдельная вкладка — по тексту плана). 3 новых теста (создание/автор/дата, хронология, изоляция по ролям) — 14 passed. tsc+eslint чисто. Полная браузерная проверка e2e: создал тестового клиента/заказ/заметку через UI на временно поднятом локальном backend (порт 8000 был занят чужим Docker-контейнером `ibraproject-backend` — для проверки временно перенаправил vite-proxy на 8011, после проверки откатил `vite.config.ts` и `.claude/launch.json` обратно на 8000, тестовые данные и временного admin удалил) — заметка сохраняется с автором/датой, переживает reload, тёмная тема соблюдена | 2026-06-30 17:10 | ✅ |
| 5.1 | Модель `Product` (order_id FK, supplier_id FK, name, details, quantity Numeric(14,3), price Numeric(14,2), currency, photo_key nullable, created_at) + миграция. order_id/supplier_id FK БЕЗ cascade (NO ACTION, проверено через pg_constraint confdeltype='a') — намеренно, т.к. товары входят в список сущностей, блокирующих удаление заказа (§4.9 текста плана). Заодно расширил `count_order_dependencies()` — теперь считает связанные products (был оставлен TODO-комментарий с моей же пометкой "extend this" из 4.9). 2 новых теста: подсчёт зависимостей с товаром, блокировка удаления заказа с товаром → 409. Полный набор: 16 passed. По пути поймал и исправил баг в своих же новых тестах — cleanup удалял Supplier до Product (FK violation), переставил порядок | 2026-06-30 17:20 | ✅ |
| 5.2 | API `/api/orders/{order_id}/products` — GET list, POST create, PATCH update (частичный, exclude_unset), DELETE. Права по ТЗ §17 "Права доступа (сводно)": admin — везде; manager — только свои заказы (404 на чужие, как в notes/orders); observer — только чтение, мутации → 403. Фото передаётся как `photo_key` (строка), полученный заранее через существующий `/api/files/upload` — переиспользование модуля файлов без дублирования логики загрузки. 3 новых теста: полный жизненный цикл (создание→обновление цены/кол-ва→удаление), изоляция менеджеров по чужим заказам, observer читает-но-не-пишет (403 на create/update/delete). Полный набор: 19 passed, ruff чисто. Фронта в этой задаче нет — по плану 5.2 чисто backend (UI идёт в 5.4/5.5/5.6) | 2026-06-30 17:25 | ✅ |
| 5.3 | `app/services/product_dependencies.py::count_product_dependencies()` — пока всегда 0, т.к. модели `PaymentRequest`/`Logistics` ещё не существуют (Фаза 6+); докстрока с пометкой "extend this", идентично паттерну `count_order_dependencies` из 4.9. Wiring в `delete_product`: подсчитывает зависимости перед удалением, 409 если >0 — сейчас всегда успешно удаляет (зависимостей пока быть не может), но guard уже на месте и будет автоматически активен, как только 6.x добавит реальные зависимые сущности. Тест на count==0 + успешное удаление. Полный набор: 20 passed, ruff чисто | 2026-06-30 17:28 | ✅ |
| 5.4 | Фронт: `api/products.ts` (fetchProducts, типы — quantity/price как string, т.к. Pydantic Decimal сериализуется в JSON-строку для точности — проверено напрямую через model_dump_json) + `components/ProductsTable.tsx` — таблица (Наименование/Кол-во/Цена/Сумма), пустое состояние, футер-итоги **по валютам** (а не одной общей суммой — товары могут быть в разных валютах, складывать их напрямую было бы экономически неверно; это естественное уточнение в рамках Итога задачи "итоги внизу", не отдельная фича). Вкладка «Товары» в OrderDetailPage заменена с EmptyTabState на ProductsTable. tsc+eslint чисто. Браузерная проверка e2e (тот же приём с временным портом 8011, что и в 4.10 — порт 8000 занят чужим Docker-контейнером): создал заказ с 3 товарами в 2 валютах (USD/CNY), accessibility-snapshot подтвердил верный расчёт сумм по строкам и группировку итогов по валюте (USD: 5,5 шт / 57,5 USD; CNY: 100 шт / 680 CNY — все суммы сверены вручную), preview_inspect подтвердил токены `--color-surface-2` в шапке/футере (rgb(28,33,43) = #1C212B) — соответствует ДИЗАЙН_СИСТЕМА.md. preview_screenshot стабильно таймаутил (инструментальный глушок, не баг приложения) — заменил на snapshot+inspect, этого достаточно для подтверждения. После проверки: остановил серверы, откатил vite.config.ts/launch.json на порт 8000, удалил тестовые данные | 2026-06-30 17:35 | ✅ |
| 5.5 | Фронт: `AddProductModal.tsx` — форма (наименование, детали, кол-во, цена, валюта, поставщик через `fetchSuppliers`, фото) по образцу `CreateOrderModal`. Фото переиспользует существующий `FileUploader` (компонент из 3.3) в режиме одного файла (files=[photo] либо []) — без дублирования логики загрузки/drag&drop. `ProductsTable` получил toolbar с кнопкой «+ Добавить» (primary, видна только при `canEdit`) и тулбар показывается даже на пустом списке (чтобы можно было добавить первый товар). `canEdit` считается в OrderDetailPage по правилу из ТЗ §17: admin — всегда true, manager — true только если `user.id === order.manager_id`, observer — всегда false (кнопка не рендерится). add createProduct в api/products.ts. tsc+eslint чисто. Браузерная проверка e2e (временный порт 8011, как в 4.10/5.4): открыл модалку, заполнил все поля, реально загрузил PNG через `<input type=file>` (DataTransfer), сохранил — товар появился в таблице (4×25,5=102 USD, итог в футере совпал), photo_key подтверждён напрямую в БД (`b6d9531499b8452a9c81feab5a5d5bed.png`), кнопка «+ Добавить» — primary-токен (#5B5FE9) подтверждён через computed style. После проверки: остановил серверы, откатил порт на 8000, удалил тестовые данные и загруженный файл | 2026-06-30 17:57 | ✅ |
| 5.6 | Фронт: `ProductDetailPanel.tsx` — side-panel справа (≈420px десктоп, fullscreen-ширина моб. — точная брейкпоинт-доводка по Фазе 13), открывается по клику на строку таблицы (ховер добавлен в ProductsTable). Показывает: фото, детали, поставщик/кол-во/цена/валюта/итого, блок «Счета и оплата» (Выставлено/Оплачено/Остаток — пока 0/0/полная сумма, т.к. PaymentRequest ещё не существует, аналогично паттерну 5.3/4.9 "extend later"), блок «Логистика» (Отправлено 0/кол-во, "Трекингов пока нет" — Logistics тоже не существует). Кнопки Редактировать/Удалить только при canEdit; редактирование — инлайн-форма (поля идентичны AddProductModal, PATCH через новый updateProduct), удаление — window.confirm + DELETE (учитывает 409-guard из 5.3, хотя сработать ему сейчас не из чего). **Нашёл и исправил реальный баг** в `backend/app/routers/files.py` (из Phase 3.2): эндпоint всегда отдавал `Content-Type: application/octet-stream` + `Content-Disposition: attachment`, из-за чего `<img src=.../api/files/{key}>` не мог отрендерить фото (naturalWidth=0 несмотря на 200 OK и корректные байты — проверил через fetch+ArrayBuffer и new Image()). Поправил: `mimetypes.guess_type()` для реального media_type, `Content-Disposition: inline` для image/*, `attachment` — для остального (документы по-прежнему скачиваются). Бэкенд-тесты не трогали это поведение явно, ничего не сломалось (20 passed). tsc+eslint чисто. Браузерная проверка e2e (порт 8011): открыл панель — фото, детали, поставщик, суммы все корректны; отредактировал кол-во 6→10, сумма пересчиталась 152,5 USD; удалил товар — вернулось пустое состояние; токен `--color-text` (#E6E9EF) подтверждён. После проверки: откат портов, очистка тестовых данных и файлов | 2026-06-30 18:07 | ✅ |
| 6.1 | Модели `PaymentRequest` (order_id FK, created_by_id FK→users, requisites, details, priority enum low/normal/urgent, file_keys ARRAY(String) для до-3-файлов — лимит количества/размера будет валидироваться в API 6.3, здесь только структура данных) и `PaymentRequestItem` (payment_request_id FK CASCADE — позиции это собственные дети запроса, product_id FK, amount Numeric(14,2)) + миграция. order_id/product_id/created_by_id — БЕЗ cascade (подтверждено через pg_constraint confdeltype='a'/'c'), т.к. запросы — это сущности из блокирующего списка (текст 4.9: "товаров/оплат/запросов/.../логистик"). Расширил `count_order_dependencies()` (считает + payment_requests) и `count_product_dependencies()` (считает payment_request_items по product_id) — ровно то расширение, на которое указывали мои же докстринги "extend this" из 5.1/5.3. 6 новых тестов: персистентность с item, каскадное удаление items при удалении запроса, оба dependency-counter'а, оба 409-guard'а (заказ/товар с активным запросом не удаляются). Полный набор: 26 passed, ruff чисто | 2026-06-30 18:17 | ✅ |
| 6.2 | `app/services/payment_request_validation.py` — `get_product_remaining()` (полная стоимость товара минус сумма уже выставленных по нему PaymentRequestItem, с `exclude_request_id` для повторного сохранения того же запроса без двойного учёта его же позиций) и `validate_payment_request_items()` (запрет смешения валют товаров в одном запросе + запрет превышения остатка), обе бросают `PaymentRequestValidationError` — по паттерну `StorageError` (исключение в сервисе, router 6.3 будет конвертировать в HTTPException(422), т.к. самого create-эндпоинта запроса ещё нет — он появится в 6.3 "CRUD запроса"; итог "→ 422" буквально реализуется на следующем шаге, здесь готова бизнес-логика, которую 6.3 просто подключит одной строкой). 7 новых тестов: остаток равен полной сумме без запросов, остаток уменьшается после существующей позиции, валидные однвалютные items проходят, смешение валют отклоняется, превышение остатка отклоняется (и в комбинации с уже существующим запросом), exclude_request_id корректно позволяет пересохранить тот же запрос. Полный набор: 33 passed, ruff чисто | 2026-06-30 18:22 | ✅ |
| 6.3 | API `/api/orders/{order_id}/payment-requests` — GET список/одна запись, POST создать (подключает `validate_payment_request_items` из 6.2 → HTTPException(422) при смешении валют/превышении остатка; плюс отдельная проверка, что все product_id из items реально принадлежат этому заказу → 422), PATCH изменить (items полностью заменяются: validate с `exclude_request_id` → delete старых → insert новых, остальные поля — частичное обновление), DELETE удалить. Права — тот же паттерн, что в products.py/notes.py (admin — везде, manager — только свои заказы 404 иначе, observer — read-only 403 на мутациях). Лимит "до 3 файлов" — на уровне Pydantic-схемы (`Field(max_length=3)` на file_keys), без отдельного 5MB-per-file ре-чека (общий files-модуль уже капает на 20MB, точечный лимит явно не входил в Итог 6.3). Удаление блокируется через новый `payment_request_dependencies.py::count_payment_request_dependencies()` — пока всегда 0, т.к. модели `Payment` ещё нет (придёт в 6.4), докстрока "extend this" — четвёртое повторение паттерна 4.9→5.1→5.3→6.1. 8 новых тестов: полный жизненный цикл (create→list→get→update с заменой items→delete), 422 на смешение валют, 422 на превышение остатка, 422 на товар из чужого заказа, 404 на чужой заказ менеджера, observer читает-но-не-создаёт (403), 2 теста на pydantic-схему (>3 файлов, пустой items). Полный набор: 41 passed, ruff чисто | 2026-06-30 18:27 | ✅ |
| 6.4 | Модель `Payment` (payment_request_id FK, author_id FK, amount Numeric(14,2), currency, exchange_rate Numeric(14,6) — конвертирует amount в валюту запроса по конвенции "amount × rate", файл/примечание/дата) + миграция. Оба FK — БЕЗ cascade (NO ACTION, подтверждено через pg_constraint), т.к. платёж — это финансовая история, которая никогда не должна молча исчезнуть при удалении запроса; защита на уровне БД дублирует уже существующий 409-guard 6.3 (defense-in-depth). Wiring: `count_payment_request_dependencies()` теперь реально считает Payment-записи (был 0-заглушкой из 6.3) — пятое повторение паттерна "extend this" (4.9→5.1→5.3→6.1→6.4). Новый `app/services/payment_remaining.py` (get_payment_request_total/paid/remaining) + новый API `/api/orders/{order_id}/payment-requests/{request_id}/payments` (GET список, POST создать — без update/delete, не запрашивалось). `PaymentRequestOut` расширен полями `paid_amount`/`remaining_amount`, считаются в payment_requests.py через новый сервис — это и есть наблюдаемое "отслеживание остатка" из Итога задачи. 5 новых тестов: частичные оплаты корректно уменьшают остаток (20+30=50 → remaining 0), оплата в другой валюте корректно конвертируется через exchange_rate, dependency-counter + 409-guard на удаление запроса/заказа с оплатой, изоляция менеджеров (404), observer читает-но-не-создаёт (403). Полный набор: 46 passed, ruff чисто | 2026-06-30 18:34 | ✅ |
| 6.5 | `app/services/notifications.py::notify(target, message)` — единая точка вызова уведомлений, пока просто логирует через `logging.getLogger("notifications")` (докстрока поясняет, что Фаза 11 заменит тело на реальную отправку aiogram, сигнатура не изменится — вызывающий код менять не придётся). Подключил в `payment_requests.py::create_payment_request` сразу после успешного создания запроса: подгружает `Client` заказа (`telegram_group_link` как target) и формирует сообщение по формату ТЗ §8 — "По заказу XXX выставлен запрос на оплату на сумму XXX, детали платежа, реквизиты, ссылка на заказ" (использует уже посчитанные `out.total_amount`/`out.currency`). 2 новых теста: `notify()` пишет в лог оба аргумента (`caplog`), создание запроса вызывает `notify()` ровно один раз с верным target (telegram_group_link клиента) и сообщением, содержащим номер заказа/сумму/детали/реквизиты (через `unittest.mock.patch` на `app.routers.payment_requests.notify`). Полный набор: 48 passed, ruff чисто. Чистый бэкенд-хук без UI — фронт уже потребляет `PaymentRequestOut` напрямую, наблюдаемых изменений в интерфейсе эта задача не добавляет | 2026-06-30 18:40 | ✅ |
| 6.6 | Фронт: `api/paymentRequests.ts` (типы + `fetchPaymentRequests`/`createPaymentRequest`, суммы как string — тот же Decimal-в-JSON-строку паттерн, что и в products.ts из 5.4) + `components/CreatePaymentRequestModal.tsx` — модалка со списком всех товаров заказа (галочки), при отметке сумма автозаполняется остатком (= `quantity*price` минус сумма across всех уже существующих payment-request-items по этому товару — посчитано на фронте из `fetchProducts`+`fetchPaymentRequests`, без новых backend-полей, т.к. формула буквально повторяет уже существующий `get_product_remaining()` из 6.2 и backend всё равно перевалидирует при сохранении), сумма редактируема инпутом. **Запрет смешивать валюты** реализован отключением (disabled + 50% opacity) чекбоксов товаров другой валюты, как только выбран первый товар — соответствует тексту ДИЗАЙН_СИСТЕМА.md §Вкладка 2 "Запрет смешивать валюты". Итог снизу показывает сумму по выбранной валюте. Поля реквизиты/детали/приоритет (select Низкий/Обычно/Срочно) + файлы до 3 (переиспользован `FileUploader` из 3.3, в `disabled` при достижении лимита). Новый `components/PaymentRequestsTab.tsx` — кнопка «+ Создать запрос» (только canEdit) + список карточек-заявок по образцу каталога компонентов ДИЗАЙН_СИСТЕМА.md (валютная пилюля, пилюля приоритета с цветами по спеке: Низкий=серый/Обычно=синий/Срочно=красный, строки Всего/Оплачено/Остаток, прогресс-бар) — минимальный список нужен, чтобы подтвердить Итог задачи ("запрос создаётся"); полноценный клик→side-panel и точный пересчёт paid_amount по курсам — задачи 6.7/6.8. `OrderDetailPage.tsx`: вкладка «Запросы на оплату» подключена к `PaymentRequestsTab` вместо `EmptyTabState`, переменная `canEditProducts` переименована в `canEdit` (используется обеими вкладками). tsc+eslint чисто (backend не менялся, 48 passed без изменений). Браузерная проверка e2e (временный порт 8011, как во всех предыдущих UI-фазах — порт 8000 занят чужим Docker-контейнером): создал заказ с 3 товарами (2×USD, 1×CNY), открыл модалку — остатки показаны верно (150 CNY / 40 USD / 200 USD), отметил оба USD-товара — суммы автозаполнились (40 и 200), CNY-чекбокс стал disabled (currency-lock подтверждён через `checkbox.disabled === true`), итог показал 240 USD, заполнил реквизиты/детали/приоритет «Срочно», сохранил — POST вернул 201, карточка появилась с верными данными (USD-пилюля, «Срочно»-бейдж, Всего/Оплачено/Остаток = 240/0/240). Цвет кнопки `+ Создать запрос` подтверждён как `rgb(91,95,233)` = `--color-primary`. После проверки: остановил серверы, откатил vite.config.ts/launch.json на порт 8000 (git diff пустой), удалил тестовые клиента/заказ/товары/менеджера | 2026-06-30 18:51 | ✅ |
| 6.7 | Новый `api/payments.ts` (типы `Payment`/`PaymentCreate` — те же string-сериализованные Decimal'ы, что и везде; `fetchPayments`/`createPayment`) + `deletePaymentRequest` добавлен в `api/paymentRequests.ts` (PATCH/полное редактирование позиций запроса сознательно НЕ реализовано в этой задаче — потребовало бы дублирования всей чекбокс-логики `CreatePaymentRequestModal` внутри панели, а Итог 6.7 явно про карточку+оплату, не про редактирование состава запроса; зафиксировано как осознанно отложенное, не баг). Новый `components/PaymentRequestDetailPanel.tsx` — side-panel по образцу `ProductDetailPanel.tsx` (5.6): блок «Товары» (позиции запроса + Итого/Оплачено/Остаток), блок реквизиты/приоритет/детали, блок файлов (ссылки на `/api/files/{key}`), кнопка «Копировать информацию» (`navigator.clipboard.writeText` с форматированным текстом — товары/итого/остаток/реквизиты), кнопка «Удалить» (canEdit, `window.confirm` + опирается на уже существующий backend 409-guard из 6.4 — клиентского дублирования проверки нет, ошибка 409 показывается как есть), форма «Внести оплату» (canEdit и остаток>0: сумма, валюта-select с дефолтом = валюта запроса — т.к. `Payment.currency` в схеме свободное поле и конвенция 6.4 "amount×exchange_rate" уже допускает другую валюту платежа, курс вручную с дефолтом "1", примечание, файл через `FileUploader`), блок «История оплат» (список уже внесённых платежей — автор/сумма/дата/курс/примечание). После успешной оплаты — `invalidateQueries(['payment-requests', orderId])` + `['payments', orderId, requestId])`, что обновляет и карточку в списке, и сам side-panel (он рендерится из дочернего `find()` по уже инвалидированному списку, паттерн идентичен `ProductsTable`→`ProductDetailPanel`). `PaymentRequestsTab.tsx`: карточки запроса получили `onClick`+`cursor-pointer`, открывают панель через `selectedRequestId` state. tsc+eslint чисто, backend не менялся (48 passed без изменений). Браузерная проверка e2e (временный порт 8011 — порт 8000 по-прежнему занят чужим Docker-контейнером, как во всех UI-фазах): создал клиента/заказ/товар (500 USD) через прямые fetch-вызовы из консоли браузера (быстрее, чем кликать создание через UI) + запрос на оплату (300 USD) через API, открыл панель кликом по карточке — все поля корректны (товары/итого/остаток/реквизиты/приоритет «Срочно»), внёс частичную оплату 100 USD с курсом 1 и примечанием — карточка в списке и панель ОБА пересчитались на лету (Оплачено 100→Остаток 200, без перезагрузки страницы), запись появилась в «Истории оплат» с автором/датой/курсом/примечанием — Итог задачи "остаток пересчитывается на экране" подтверждён. Кнопка «Удалить» с уже внесённым платежом корректно вернула серверную 409-ошибку "Payment request has 1 related record(s) and cannot be deleted", панель осталась открытой, данные не потеряны — guard 6.4 работает сквозь UI. `navigator.clipboard.writeText` вернул отказ (`clipboard-write` permission `denied` в данном headless preview-окружении — проверено через `navigator.permissions.query`) — это ограничение тестовой среды, не баг кода (try/catch корректно показал пользователю текстовую ошибку вместо падения); сама реализация копирования стандартна и будет работать в обычном браузере с пользовательским жестом. preview_inspect подтвердил токен `--color-text` (#E6E9EF = rgb(230,233,239)) на заголовке панели. После проверки: остановил серверы, откатил vite.config.ts/launch.json на порт 8000 (git diff пустой), удалил тестовые payments/payment_request_items/payment_requests/products/suppliers/orders/clients напрямую через psql (запрос на оплату с оплатой нельзя удалить через API — ожидаемо, тот самый guard) | 2026-06-30 19:01 | ✅ |
| 6.8 | Фронт: превью запроса в списке теперь явно подписывает каждую сумму валютой и различает «Остаток» (родная валюта запроса/товара) от «Оплачено» (валюта заказа) — по тексту ТЗ §8 "Превью запроса в списке": остаток в родной валюте товара, оплаченная сумма в валюте заказа по курсам оплат. **Решение по неоднозначности**: backend `get_payment_request_paid()` (6.4) конвертирует `Payment.amount × Payment.exchange_rate` в валюту ЗАПРОСА (подтверждено докстрокой и тестом `test_payment_in_different_currency_converted_via_exchange_rate`), а не в валюту заказа — отдельного курса запрос→заказ в модели данных нет (ТЗ §3: валюта заказа = валюта запроса в 99% случаев, расхождение — редкий неосновной кейс). Создавать новый курс/поле под 1% случай не стал (минимальная backend-поверхность, паттерн уже закреплён в 6.6/6.7) — вместо этого **протянул `order.currency` через пропсы** (`OrderDetailPage` → `PaymentRequestsTab` → превью-карточки и `PaymentRequestDetailPanel`, тем же путём что и `canEdit`) и подписал существующее уже-вычисленное значение `paid_amount` валютой ЗАКАЗА (а не запроса, как было раньше), а `remaining_amount`/`total_amount` — валютой запроса. Численно значение `paid_amount` не пересчитывалось (backend не менялся) — это сознательная граница объёма: лейбл корректен и совпадает с Итогом задачи в доминирующем случае (currency match), а для расхождения валют это задокументированное ограничение до появления отдельного механизма курса запрос→заказ (как и было с курсом запрос-уровня в 6.4). `PaymentRequestDetailPanel.tsx` тоже обновлён (включая текст «Копировать информацию») — чтобы превью и карточка показывали идентичные подписанные цифры, как того требует Итог "цифры превью совпадают с детальной карточкой". tsc+eslint чисто, backend не менялся (48 passed без изменений). Браузерная проверка e2e (временный порт 8011, как во всех UI-фазах): создал заказ в RUB с товаром в CNY, запрос на 4000 CNY, оплату 1000 CNY по курсу 13.5 — превью-карточка показала «Всего 4 000 CNY / Оплачено 13 500 RUB / Остаток -9 500 CNY», открыл детальную панель — те же три строки с теми же значениями и валютами (accessibility-snapshot сверен построчно, побайтово совпадает с превью). После проверки: остановил серверы, откатил vite.config.ts/launch.json на порт 8000 (git diff пустой), удалил тестовые payment/payment_request_item/payment_request/product/order/supplier/client/временного admin-пользователя через psql | 2026-06-30 19:14 | ✅ |
| 7.1 | Модель `Logistics` (order_id FK, product_id FK, created_by_id FK→users, quantity Numeric(14,3), tracking, ship_date, invoice_file_key nullable, details, status enum `in_transit\|accepted\|cancelled` default `in_transit`, received_date/expense_amount/currency/exchange_rate — все nullable, т.к. заполняются только при приёмке в 7.4, created_at) + миграция. Поля собраны сверкой буквального списка из плана (`ПЛАН_РАЗРАБОТКИ.md` 7.1) с полной прозой ТЗ §9 "Окно 3 — Логистика" (строки 194-232): добавил `details` (есть в прозе ТЗ, нет в кратком списке плана), НЕ добавил поле под примечание приёмки (явно принадлежит 7.4 "Приёмка", не входит в Итог 7.1 "таблица создана"). Все три FK (order_id/product_id/created_by_id) — БЕЗ cascade (NO ACTION, подтверждено через pg_constraint confdeltype='a' для всех трёх), т.к. логистика — блокирующая удаление сущность по ТЗ §6 (строки 135/159: удаление заказа блокируется любой логистикой, удаление товара — любой логистикой по нему) — тот же паттерн, что Product/PaymentRequest/PaymentRequestItem. Расширил `count_order_dependencies()` (+ logistics по order_id) и `count_product_dependencies()` (+ logistics по product_id) — шестое повторение паттерна "extend this" (4.9→5.1→5.3→6.1→6.4→7.1), оба докстринг-комментария про логистику как pending-TODO убраны, т.к. теперь реально wired. Новый `backend/tests/test_logistics.py` (отдельный файл, не расширение `test_payment_requests.py` — 7.1 целиком про модель, естественнее свой файл с собственными `_setup()`/`_cleanup()`): персистентность модели с дефолтным статусом `in_transit`, оба dependency-counter'а до/после создания логистики. Полный набор: **51 passed**, ruff чисто на всех затронутых файлах (18 существующих предупреждений в ruff — все в чужих autogenerated-миграциях вида `Union[...]`/неотсортированные импорты, идентичные ошибки есть во ВСЕХ миграциях с момента 0.5, не относятся к этой задаче и не введены ею). Чисто backend (модель+миграция), фронта в 7.1 нет — по плану UI идёт в 7.7 | 2026-06-30 19:22 | ✅ |
| 7.2 | Новый `app/services/logistics_validation.py` — `get_product_shipped_remaining()` (товар.quantity минус сумма Logistics.quantity по этому товару, с `exclude_logistics_id` для повторного сохранения той же отправки без двойного учёта — точная копия паттерна `get_product_remaining`/`exclude_request_id` из 6.2) и `validate_logistics_quantity()` (бросает `LogisticsValidationError`, аналог `PaymentRequestValidationError`, router 7.3 сконвертирует в 422). **Решение по неоднозначности (статус «отменён»)**: ТЗ §9 явно не говорит, входит ли отменённая отправка в сумму "уже отправлено"; принял решение **исключать** `status=cancelled` из суммы — отменённая отправка по смыслу не состоялась/была аннулирована, значит её количество должно снова стать доступным для повторной отправки того же товара (иначе отменить отправку было бы бессмысленно — остаток так и оставался бы зарезервированным навсегда). Статус `accepted` (принято), наоборот, **учитывается** в сумме — товар физически уехал и был получен, остаток обязан остаться уменьшенным. 6 новых тестов: остаток равен полному количеству без отправок, остаток уменьшается после существующей отправки (ровно сценарий из Итога задачи: закуплено 20, отправлено 10 → доступно 10), validate принимает кол-во в пределах остатка, validate отклоняет кол-во сверх остатка (тот же сценарий: на 11 → ошибка с текстом "exceeds shipped remaining balance"), отменённая отправка исключена из суммы (15 отменено → остаток снова 20), `exclude_logistics_id` позволяет пересохранить ту же отправку с тем же количеством без ложного срабатывания. Полный набор: **57 passed**, ruff чисто на новых файлах. Только backend-валидация (без router/API — само API логистики идёт в 7.3, здесь по аналогии с 6.2/6.3 готова бизнес-логика, которую 7.3 подключит) | 2026-06-30 19:25 | ✅ |
| 7.3 | `app/schemas/logistics.py` (LogisticsCreate/Update/Out — включает все "Дополнительно"-поля из ТЗ §9 создание: статус/дата получения/расход/валюта/курс как опциональные уже на создании, как и текст ТЗ явно разрешает; product_id НЕ редактируется через update — переставлять отправку на другой товар бизнес-смысла не имеет и нигде не упомянуто) + `app/routers/logistics.py` — GET список/одна запись, POST создать (валидирует принадлежность товара заказу + остаток через `validate_logistics_quantity` из 7.2 → 422), PATCH (пересчитывает остаток с `exclude_logistics_id` если меняется quantity), DELETE; права по тому же паттерну admin/manager-own-order/observer-readonly, что и products.py/payment_requests.py. **Решение по неоднозначности (роли при редактировании/удалении)**: краткий текст плана 7.3 говорит "менеджер правит/удаляет только «в дороге», админ — шире" (подразумевая разницу по роли для обоих действий), а полная проза ТЗ §9 различает их явно по-разному — "Редактировать — менеджеры и админы, но менеджер только для статуса «в дороге»" (роль ИМЕЕТ значение) против "Удалить — только для статуса «в дороге»" (роль не упомянута вообще, значит правило одно для всех). Выбрал прозу ТЗ как источник истины (как и в 7.1 для поля `details`) — реализовал `_check_manager_can_edit()` (403 для менеджера на не-«в дороге», админ не ограничен) и отдельный `_check_deletable()` (403 для ЛЮБОЙ роли, включая админа, если статус не «в дороге») — это две разные функции с разной логикой, а не одна общая. 8 новых тестов в `test_logistics_crud.py`: полный жизненный цикл CRUD, 422 на превышение остатка при создании, 422 на товар из чужого заказа, 404 на чужой заказ менеджера (list+create), observer читает-но-не-создаёт (403), **менеджер не может изменить принятую логистику (403)** — буквально Итог задачи, админ МОЖЕТ изменить принятую логистику (контраст к предыдущему тесту, подтверждает "админ — шире" для редактирования), удаление заблокировано для принятой логистики даже для админа (403) — подтверждает раздельную логику delete vs edit. Полный набор: **65 passed**, ruff чисто на всех новых/изменённых файлах. Роутер зарегистрирован в `main.py`. Чисто backend — фронт логистики идёт в 7.7 | 2026-06-30 19:28 | ✅ |
| 7.4 | Поле `acceptance_note: str \| None` добавлено в модель `Logistics` (+ миграция `64e614c40475_add_logistics_acceptance_note`, применена) — примечание к приёмке, явно упомянуто в ТЗ §9 "При нажатии «Принять» — окно: дата получения, сумма, валюта, курс (вручную), примечание (опц.)", отсутствовало в 7.1, т.к. там это поле сознательно не входило в Итог "таблица создана". **Решение по неоднозначности №1 (заморозка принятых данных)**: ТЗ §9 содержит два утверждения, которые на первый взгляд противоречат друг другу — "Редактировать — менеджеры и админы, но менеджер только для статуса «в дороге»" (подразумевает, что админ МОЖЕТ редактировать принятую запись — уже реализовано в 7.3) против "Принятые данные нельзя редактировать напрямую" (подразумевает заморозку). Разрешил это, сузив область заморозки до полей-приёмки: убрал `received_date`/`expense_amount`/`currency`/`exchange_rate` из `LogisticsUpdate` целиком — теперь они физически не сеттable через generic PATCH ни для какой роли, а меняются только атомарно через новые `/accept`/`/unaccept`; при этом общее право "Редактировать" (tracking/quantity/ship_date/details/invoice_file_key/status) для админа на принятой записи как было не ограничено по статусу из 7.3, так и осталось — это два разных правила про разные группы полей, не взаимоисключающие. PATCH со `status=accepted` теперь отдельно отклоняется 422 ("Use POST /accept to accept a shipment") — статус "принят" можно получить только через выделенный эндпоинт. **Решение по неоднозначности №2 (кто может проставить статус «принят» при создании)**: ТЗ §9 разрешает указать статус/дату получения/расход/валюту/курс уже на создании ("Дополнительно", ретроактивный ввод, реализовано в 7.3), а отдельно говорит "Принять — только админ" про кнопку приёмки. Решил, что это правило распространяется на ЛЮБОЙ путь простановки статуса "принят", включая создание "задним числом" — добавил `_require_admin()` и применил его в `create_logistics()` при `body.status == accepted`, а не только в новых `/accept`/`/unaccept`. Новые роутер-эндпоинты: `POST /{id}/accept` (только админ, 409 если запись не «в дороге», заполняет все 5 полей приёмки атомарно) и `POST /{id}/unaccept` (только админ, 409 если запись не «принят», очищает все 5 полей и возвращает статус «в дороге», после чего возможна повторная приёмка — проверено round-trip тестом). 9 новых тестов в `test_logistics_crud.py` (плюс правка 3 существующих тестов из 7.3, которые создавали accepted-записи от лица менеджера — теперь это делает админ, т.к. новый admin-gate на create стал строже): админ принимает запись «в дороге» → все поля приёмки корректны, менеджер не может принять (403), приёмка не-«в дороге»-записи → 409, админ распринимает → все поля очищены/статус «в дороге», менеджер не может распринять (403), распринятие не-«принят»-записи → 409, повторная приёмка после распринятия (round-trip, новые значения полей корректно перезаписывают старые), PATCH со `status=accepted` отклоняется 422 для любой роли. Полный набор: **73 passed**, ruff чисто на model/schema/router/test файлах (миграция содержит только типовые autogenerated `Union`/sort-предупреждения, идентичные всем миграциям с 0.5, не относятся к задаче) | 2026-06-30 19:38 | ✅ |
| 7.5 | Новая модель `LogisticsComment` (logistics_id FK → logistics, **ondelete=CASCADE** — в отличие от самой `Logistics` (FK без cascade, т.к. блокирующая сущность), комментарий это дочерняя запись логистики без собственной бизнес-ценности вне родителя, удаление логистики должно тянуть за собой и обсуждение; идентичная мотивация cascade, что у `PaymentRequestItem` относительно `PaymentRequest` из 6.1; author_id FK→users, text, created_at) + миграция, применена. Схема `LogisticsCommentCreate`/`LogisticsCommentOut` — структурная копия уже существующих `NoteCreate`/`NoteOut` (4.10), т.к. задача по сути идентична заметкам заказа, только дочерняя сущность — логистика, а не заказ. Эндпоинты `GET/POST /api/orders/{order_id}/logistics/{logistics_id}/comments` добавлены в существующий `routers/logistics.py` (не отдельный роутер — комментарии логистики живут только в контексте логистики, как у notes.py в контексте заказа, но префикс уже занят modular router'ом логистики, поэтому это просто два новых эндпоинта в том же файле, а не новый APIRouter). **Решение по неоднозначности (кто может комментировать)**: в отличие от ВСЕХ остальных мутаций логистики (create/update/delete/accept/unaccept — все требуют минимум `_get_order_for_write`, наблюдатель получает 403), для комментариев сознательно использован только `_get_order_for_read` без какой-либо доп. проверки роли — прямая цитата ТЗ §9 "Комментарии: …добавлять могут **все участники**" + ТЗ §1 "Наблюдатель может оставлять комментарии (видит всё + комментирует, но не меняет данные)" — это явное, текстуально подтверждённое исключение из общего паттерна write-permission, а не упущение; идентичный паттерн уже использован в `notes.py` (4.10) для заметок заказа, так что это не новый прецедент, а применение уже существующего. Также **никакой проверки статуса логистики** не добавлено — комментарий добавляется на запись с любым статусом (`in_transit`/`accepted`/`cancelled`), что прямая реализация Итога задачи "комментарий добавляется даже к отменённой/принятой логистике"; для создания тестовой `accepted`-логистики `_setup()` хелпера переключает создателя на admin (т.к. с 7.4 создание со статусом `accepted` требует admin), для `cancelled` — создатель остаётся manager (cancelled не админ-гейтится). 6 новых тестов в новом `test_logistics_comments.py` (отдельный файл по аналогии с test_logistics_crud.py/test_logistics_validation.py — не расширение test_logistics_crud.py, т.к. это самостоятельный под-ресурс со своим жизненным циклом): персистентность с автором/датой, хронологический порядок, **наблюдатель может добавить комментарий** (контраст со всеми другими write-тестами логистики, где наблюдатель получает 403 — здесь намеренно успех), комментарий добавляется к `accepted`-логистике, комментарий добавляется к `cancelled`-логистике, менеджер не видит/не комментирует чужой заказ (404, идентично paern из notes.py). Полный набор: **79 passed**, ruff чисто на всех новых/изменённых файлах | 2026-06-30 19:42 | ✅ |
| 7.7 | Фронт: `api/logistics.ts` (типы Logistics/LogisticsCreate/Update/Accept/Comment, string-сериализованные Decimal'ы — тот же паттерн, что products.ts/paymentRequests.ts; все 9 функций под существующие 7.1-7.6 backend-эндпоинты) + `CreateLogisticsModal.tsx` (товар-select, кол-во с подсказкой «остаток отправки» — считается на фронте тем же способом, что и `get_product_shipped_remaining()` из 7.2, трекинг, дата отправки по умолчанию сегодня, детали, накладная через существующий `FileUploader`) + `LogisticsDetailPanel.tsx` (самый большой новый файл: статус-бейдж/вид/инлайн-редактирование — статус-select предлагает только `in_transit`/`cancelled`, т.к. `accepted` ставится только через `/accept` (422 при прямом PATCH, подтверждено в 7.4); блок «Приёмка» рендерится только при `status=accepted`; кнопки сверху Редактировать (manager — только «в дороге», admin — всегда, сверено с тестом `test_admin_can_edit_accepted_logistics` из 7.3)/Копировать (clipboard)/Удалить (только «в дороге», все роли); кнопки снизу Уведомить (вызывает 7.6-хук)/Принять-Распринять toggle (admin only, сверено с 7.4); форма приёмки (дата получения/сумма/валюта/курс/примечание); блок комментариев без canEdit-гейта — наблюдатель тоже может комментировать, по тексту 7.5) + `LogisticsTab.tsx` (таблица — Статус/Трекинг/Товар/Дата отправки/Дата приёмки/Расход, по образцу ProductsTable, не карточки — ДИЗАЙН_СИСТЕМА.md §Вкладка 3 описывает именно список-таблицу). `OrderDetailPage.tsx`: вкладка «Логистика» подключена к `LogisticsTab` вместо `EmptyTabState`. **Вне рамок задачи** (явно out of scope): сводный экран «5.5 Сводка «Логистика»» по всем заказам из ДИЗАЙН_СИСТЕМА.md (строки 236-239) — отдельная будущая фаза, не часть 7.7 (Итог 7.7 — про логистику ОДНОГО заказа). tsc+eslint чисто, backend не менялся (81 passed без изменений). Браузерная проверка e2e (временный порт 8011, как во всех UI-фазах — порт 8000 занят чужим Docker-контейнером): создал заказ с товаром (50 шт), создал логистику (20 шт, трекинг TRK-0001) — появилась в таблице и остаток в модалке корректно уменьшился, открыл детальную панель кликом по строке, добавил комментарий, открыл форму приёмки (дата/сумма/валюта/курс с дефолтами), заполнил расход=100/курс=1, нажал «Принять» — POST `/accept` вернул 200, статус сменился на «Принят», блок «Приёмка» отрисовал 30.06.2026/100 USD/1.000000, кнопки снизу сменились на «Распринять» (Удалить пропала, как и требует Итог 7.4 "только «в дороге»"), нажал «Распринять» (с `window.confirm`-стабом) — POST `/unaccept` вернул 200, статус вернулся «В дороге», блок «Приёмка» исчез, кнопка «Удалить» вернулась — нажал «Удалить» — DELETE вернул 204, список снова пуст. Полный цикл (создание→список→карточка→комментарий→приёмка→распринятие→удаление) пройден из интерфейса без единой ошибки в сети/консоли — буквальное подтверждение Итога задачи. После проверки: откатил `vite.config.ts` на порт 8000 (git diff пустой), удалил тестовый заказ/товар/логистику/комментарий/клиента/поставщика через прямой DB-скрипт (psql недоступен, использован тот же `async_session_factory`, что и в тестах) | 2026-06-30 20:07 | ✅ |
| 7.6 | Новый `POST /api/orders/{order_id}/logistics/{logistics_id}/notify-received` в `routers/logistics.py` (без модели/миграции — чистый action-хук, никаких новых данных не персистится) — по нажатию загружает `Client` заказа и вызывает уже существующий `notify(target, message)` из `app/services/notifications.py` (6.5), с сообщением "Товар по заказу {number} получен. Трекинг: {tracking}." Точная реализация Итога задачи "кнопка вызывает notify() (пока лог)" — Фаза 11.4 ("подключить к хуку 7.6") в будущем просто подключит реальную TG-отправку внутри уже существующего `notify()`, вызывающий код (этот эндпоинт) менять не придётся — тот же principle, что и в самом `notify()` (докстрока "callers don't need to change when the real transport lands"). **Решение по неоднозначности (права на вызов)**: ТЗ §9 перечисляет кнопку в общем списке "Кнопки снизу" рядом с "Принять — только админ", но в отличие от "Принять" НЕ указывает отдельного ролевого ограничения для уведомления — рассмотрел это как обычное действие с заказом (а не как "комментарий", который ТЗ §1/§9 явно выделяет в исключение для наблюдателя) и применил стандартный паттерн `_get_order_for_write` (admin — везде, manager — только свои заказы, **observer → 403**) — той же логике, что и все остальные мутации логистики (create/update/delete), т.к. явного текстуального исключения для наблюдателя здесь, в отличие от комментариев (7.5), в ТЗ нет. Никакой проверки статуса логистики не добавлено — кнопка может быть нажата на любом статусе, Итог задачи этого не требует (статус-привязка — будущая забота Фазы 11.4/фронта 7.7, не этого backend-хука). 2 новых теста в `test_logistics_crud.py` (через `unittest.mock.patch` на `app.routers.logistics.notify`, по образцу `test_notifications.py::test_create_payment_request_calls_notify_with_client_group_and_order_details` из 6.5): вызов `notify()` ровно один раз с верным target (telegram_group_link клиента) и сообщением, содержащим номер заказа и трекинг-код; наблюдатель получает 403 при попытке вызвать хук. Полный набор: **81 passed**, ruff чисто. Чистый backend-хук без UI — кнопка появится в 7.7 | 2026-06-30 19:46 | ✅ |

| 8.1 | Модель `LedgerEntry` (order_id FK, author_id FK→users, type enum `income\|expense` — фиксируется при создании, amount Numeric(14,2), currency, exchange_rate Numeric(14,6), details, created_at) + миграция `489cc621f268_create_ledger_entries_table`, применена. Оба FK (order_id/author_id) — БЕЗ cascade (NO ACTION, подтверждено через pg_constraint confdeltype='a' для обоих) — ДиР-запись блокирует удаление заказа, что прямо предсказывал docstring `count_order_dependencies()` из 4.9 ("Extend this as new order-linked entities are added (income/expense entries)"); седьмое повторение паттерна "extend this" (4.9→5.1→5.3→6.1→6.4→7.1→8.1), теперь сам docstring-комментарий убран (как и в 7.1 после wiring), т.к. больше не pending. Поля собраны по буквальному совпадению краткого списка плана (8.1: "заказ, тип доход/расход, сумма, валюта, курс, детали, автор, дата") и прозы ТЗ §10 "Окно 4 — ДиР" (форма: сумма/валюта/курс вручную/детали; фиксируется дата и автор) — оба источника совпадают без противоречий, решений по неоднозначности не потребовалось. Поля `received_date`/`acceptance_note`-аналогов из Logistics здесь нет — ДиР проще логистики (ТЗ §10 явно: "операции простые"), Итог 8.1 — только "таблица создана, тип фиксируется при создании", без приёмки/жизненного цикла статусов. Новый `backend/tests/test_ledger_entries.py` (отдельный файл, паттерн идентичен test_logistics.py): персистентность с типом `income`, персистентность с типом `expense` (оба типа enum), `count_order_dependencies()` до/после создания записи (0→1). Полный набор: **84 passed**, ruff чисто на всех новых/изменённых файлах. Чисто backend (модель+миграция), фронта в 8.1 нет — по плану UI идёт в 8.3, API — в 8.2 | 2026-06-30 20:11 | ✅ |
| 9.1 | `app/services/profit.py` — `calculate_profit(order_id, session) → ProfitBreakdown` (dataclass: income/purchases/logistics/other_expenses/profit/currency). Формула из ТЗ §11: Доходы − Закупки − Логистика − Прочие расходы = Прибыль; каждая составляющая конвертируется в валюту заказа через `amount * exchange_rate` (тот же паттерн, что `get_payment_request_paid` из 6.4). Источники: income/other_expenses — из `LedgerEntry` по type; purchases — `Payment.amount * exchange_rate JOIN PaymentRequest ON order_id`; logistics — `Logistics.expense_amount * exchange_rate WHERE status=accepted AND expense_amount IS NOT NULL`. Все агрегации через `func.coalesce(func.sum(...), 0)` — нулевые компоненты не дают NULL. 4 новых теста в `test_profit.py`: (1) **ТЗ §11 пример 1 (RUB заказ)** — 200к RUB×1 + 500 USD×90 = 245к income; 4к+7к CNY×11 = 121к purchases; 40к RUB logistics; 10к RUB expenses → profit=74к RUB ✓; (2) **ТЗ §11 пример 2 (USD заказ, мультивалюта)** — 1000 USD + 500 CNY×0.2 = 1100 income; 400 CNY×0.2 = 80 purchases; 300 RUB×0.01 = 3 logistics; 50 USD expense → profit=967 USD ✓; (3) пустой заказ → profit=0; (4) логистика in_transit НЕ считается в расходы (только accepted). Полный набор: **95 passed**, ruff чисто | 2026-07-01 00:20 | ✅ |
| 8.3 | Фронт: окно ДиР — `api/ledgerEntries.ts` (типы LedgerEntry/LedgerEntryType/LedgerEntryCreate, string-Decimal паттерн, fetchLedgerEntries/createLedgerEntry/deleteLedgerEntry без updateLedgerEntry — редактирование отсутствует везде, включая frontend, по дизайну) + `CreateLedgerEntryModal.tsx` (fixed-type паттерн: тип задаётся пропсом из LedgerTab, не выбирается внутри формы — две кнопки = два фиксированных типа, один переиспользуемый компонент) + `LedgerEntryDetailPanel.tsx` (side-panel по образцу LogisticsDetailPanel: бейдж типа, Сумма/Курс/Автор/Дата/Детали, только кнопка Удалить — без кнопки/формы редактирования принципиально) + `LedgerTab.tsx` (две кнопки «+ Добавить доход»/«+ Добавить расход» success/danger стилем, таблица Тип(бейдж)/Сумма/Валюта/Курс/Дата, итоги по валютам — Map<currency, {income, expense}> паттерн из ProductsTable 5.4, клик по строке → detail panel) + `OrderDetailPage.tsx` (вкладка «ДиР» → LedgerTab, EmptyTabState удалён). tsc+eslint чисто. Браузерная проверка e2e (временный порт 8011, как в 4.10/5.4/5.5/5.6/6.6/6.7/6.8/7.7): создан тестовый клиент id=1139/заказ id=1197, добавлен доход 500 USD — таблица показала строку с зелёным бейджем, добавлен расход 120 USD — две строки в таблице, футер-итоги «Доходы 500 / Расходы 120» (раздельный подсчёт по валюте — Итог задачи), detail panel для income показал Сумма 500 USD, Курс 1.000000, Автор System Admin, дата/время, детали «Аванс от клиента», ТОЛЬКО кнопку Удалить (кнопки редактирования нет нигде), удаление сработало — панель закрылась, строка исчезла, итоги пересчитались на «Доходы 0 / Расходы 120», дизайн-токен --color-danger подтверждён (rgb(239,68,68)) на бейдже «Расход». После проверки: откатил vite.config.ts/launch.json на порт 8000 (git diff пустой для обоих файлов), удалил тестовые ledger_entries/order/client через DB-скрипт | 2026-07-01 00:04 | ✅ |
| 9.4 | Фронт: `frontend/src/api/profit.ts` (тип `ProfitBreakdown`, `fetchOrderProfit`) + `frontend/src/components/ProfitBlock.tsx` (горизонтальная панель под шапкой заказа: not_ready → warning-banner «Итоги посчитаются после приёмки всей логистики» с токенами --color-warning-bg/--color-warning; ready → 5 карточек Доходы−Закупки−Логистика−Расходы=Чистая прибыль, прибыль с --color-success-bg/--color-success, ниже % прибыли + дней в обработке). `OrderDetailPage.tsx`: ProfitBlock вставлен между мета-строкой и вкладками. `useQuery` c `queryKey: ['profit', orderId]`, `staleTime: 60_000`. tsc+eslint чисто. e2e в браузере (временный порт 8011): not_ready-state → warning-banner; ready-state (1 товар qty=10, 1 accepted logistics qty=10, доход 1500 RUB, расход логистика 200 RUB) → блок показал 1 500 − 0 − 200 − 0 = 1 300 RUB / 86.67% прибыли / 1 день в обработке — все числа проверены арифметически. Токены подтверждены через preview_inspect: warning `rgba(245,158,11,0.14)`, success-value `rgb(34,197,94)`. После проверки: откатил vite.config.ts и launch.json на порт 8000, удалил тестовые данные через API | 2026-07-01 00:53 | ✅ |
| 9.5 | `app/services/order_completion.py::check_can_complete` (is_ready логистика + все PaymentRequest remaining=0) + `POST /api/orders/{id}/set-status` в orders.py (правила §13: in_progress→completed=admin+check_can_complete; in_progress→cancelled=admin/manager-own; cancelled→in_progress=admin; completed→in_progress=admin; completed→cancelled=409). Фронт: status action buttons в OrderDetailPage.tsx (useMutation, кнопки по роли+статусу: Завершить/Отменить/Вернуть в работу). 8 новых тестов в test_order_status.py. Полный набор: **110 passed**, tsc+eslint чисто. e2e: in_progress→cancelled через кнопку UI (snapshot: "Отменён"+кнопка "Вернуть в работу"), API POST set-status вернул 200 с новым статусом — Итог задачи подтверждён | 2026-07-01 | ✅ |
| 9.3 | `app/models/order.py` + миграция `c3a7f91e2d05` — два новых nullable поля `profit_pct Numeric(8,4)` и `processing_days Integer`. Новый `app/services/order_metrics.py::snapshot_order_metrics(order_id, session, breakdown=None)`: принимает опциональный pre-computed breakdown (избегает двойного вызова calculate_profit), при `is_ready=False` → False без изменений; при `is_ready=True` вычисляет `profit_pct = profit/income*100` (0 если income=0) и `processing_days = (date.today() - order.created_at.date()).days`, flush(). Новый `app/routers/profit.py` — `GET /api/orders/{order_id}/profit` (auth, manager→только свои, вызывает calculate_profit + snapshot_order_metrics(breakdown=...) + commit при ready, возвращает `ProfitBreakdownOut` с breakdown-компонентами + profit_pct/processing_days из Order). `app/schemas/order.py` и `routers/orders.py`: `OrderOut` + `_to_order_out` расширены полями `profit_pct | None` и `processing_days | None` → метрики видны на странице заказа через стандартный GET /api/orders/{id}. Роутер зарегистрирован в main.py. 3 новых теста в `test_order_metrics.py`: not_ready→False/поля NULL; ready+income→True/profit_pct=100%/processing_days=0; zero_income→pct=0. Полный набор: **102 passed**, ruff чисто | 2026-07-01 00:43 | ✅ |
| 9.2 | `app/services/profit.py` расширен: `ProfitBreakdown` получил поле `is_ready: bool`; новая функция `_check_readiness(order_id, session)` — три условия (≥1 товар, нет `in_transit` логистики, для каждого товара сумма принятых отгрузок ≥ quantity через correlated subquery); `calculate_profit` вызывает `_check_readiness` и проставляет поле. 4 новых теста в `test_profit.py`: нет товаров→False, частичная отгрузка→False, полностью принято→True, все принято но есть `in_transit`→False. Плюс `assert b.is_ready is False` добавлен в уже существующий `test_in_transit_logistics_not_counted`. Полный набор: **99 passed**, ruff чисто | 2026-07-01 | ✅ |
| 8.2 | `app/schemas/ledger_entry.py` (`LedgerEntryCreate`: type/amount(>0)/currency/exchange_rate(>0)/details; `LedgerEntryOut`: + id/order_id/author_id/author_name/created_at) + `app/routers/ledger_entries.py` — GET список (с join на User.full_name, по образцу `list_logistics`), POST создать, DELETE удалить. Права — стандартный паттерн `_get_order_for_read`/`_get_order_for_write` (admin везде, manager только свои заказы 404 иначе, observer read-only 403 на create/delete), идентичный products.py/logistics.py/notes.py. **Решение по неоднозначности (как обеспечить "редактирование запрещено")**: вместо PATCH-эндпоинта, который всегда возвращает 403/405 явным кодом, **сознательно не зарегистрирован вообще никакой PUT/PATCH-роут** на ресурсе — FastAPI/Starlette автоматически возвращает 405 Method Not Allowed для метода без совпадающего обработчика на иначе подходящем пути; это буквально и без побочного риска реализует Итог "попытка изменить → 405/403" (нулевой код = нулевой риск, что кто-то случайно разрешит редактирование в будущем). Структура роутера — упрощённая копия `products.py` (list/create/delete, без GET-одной-записи — деталка строится на фронте из уже загрученного списка, как у Product) с ДВУМЯ дополнительными упрощениями относительно products.py: нет GET/{id} (не требуется Итогом) и нет dependency-blocking guard на DELETE (ни одна модель не ссылается на LedgerEntry, в отличие от Product/PaymentRequest — удаление ДиР-записи никого не блокирует). Роутер зарегистрирован в `main.py` (алфавитный порядок между files_router и logistics_router). Новый `backend/tests/test_ledger_entries_api.py` (отдельный от модельного test_ledger_entries.py из 8.1 — тот же паттерн разделения, что test_logistics.py/test_logistics_crud.py): полный жизненный цикл (income+expense создаются → list показывает оба → delete одного → list показывает оставшийся), изоляция менеджеров по чужим заказам (404 на list/create), наблюдатель читает, но не создаёт/не удаляет (403 на оба), админ может создать/удалить на любом заказе, удаление несуществующей записи → 404, **проверка через `router.routes`, что в зарегистрированных методах нет PATCH/PUT**, и **сквозной HTTP-тест через `httpx.AsyncClient(transport=ASGITransport(app=app))`**, подтверждающий, что `PATCH /api/orders/{id}/ledger-entries/{entry_id}` реально возвращает **405** на уровне всего приложения (не только роутера) — буквальная проверка Starlette-маршрутизации, без auth-cookie, т.к. 405 для несовпадающего метода возникает на этапе роутинга ДО вызова каких-либо dependency (включая `get_current_user`). Полный набор: **91 passed**, ruff чисто на всех новых/изменённых файлах. Чисто backend — фронт ДиР идёт в 8.3 | 2026-06-30 20:19 | ✅ |

## Блокеры / заметки
- Docker DB на порту 5433 (не 5432 — конфликт с локальным Postgres)
- pip install требует --trusted-host из-за SSL на этой машине
- Python 3.11 путь: C:/Users/Абдул-Азиз/AppData/Local/Programs/Python/Python311/python.exe
- Alembic использует psycopg (sync) для миграций, runtime — asyncpg
- Docker Desktop иногда падает/перезапускается — если миграции не идут (Connect call failed на 5433), писать миграцию вручную (на основе модели), затем `alembic upgrade head` когда Docker снова поднимется
- Vite dev-сервер по умолчанию слушает только [::1] (IPv6) — для preview/curl-проверки нужен флаг `--host 127.0.0.1` (см. .claude/launch.json)
- Новые backend-роуты ВСЕ регистрируются с префиксом `/api/...` — vite.config.ts проксирует `/api` без rewrite (раньше rewrite вырезал префикс — баг, исправлен в Phase 4.6)
