# Дизайн-система и поэкранный план — Система внутреннего учёта заказов

> Этот документ — «дизайнерская» половина полного плана. Он фиксирует визуальный язык по референс-скриншотам (тёмная тема первого проекта) и расписывает каждый экран новой системы.
> Парный документ — [ПЛАН_РАЗРАБОТКИ.md](ПЛАН_РАЗРАБОТКИ.md) (сборка по фазам). Источник требований — [ТЗ (обновлённое)](<ТЗ на проект подсчета заказов (обновленное).md>).

## Решения по дизайну (зафиксировано)

1. **Тема:** тёмная по умолчанию (как на скриншотах) + светлая по переключателю. Токены для обеих.
2. **Детальная страница заказа:** гибрид — шапка + блок прибыли сверху, ниже переключаемые вкладки **Товары → Запросы на оплату → Логистика → ДиР**. Внутри вкладок — визуал со скриншотов (тёмная таблица номенклатуры, карточки заявок, боковая панель деталей записи).
3. **Терминология:** в системе используем термины ТЗ (Заказы, Товары, Запросы на оплату), а не первого проекта (Проекты, Номенклатура, Заявки). Визуальный стиль берём со скриншотов.

---

## 1. Дизайн-принципы

- **Плотно, но дышит.** Много данных (деньги, валюты, остатки) → компактные таблицы и карточки, но с воздухом между блоками. Опора на скриншоты: тёмный фон, карточки чуть светлее, тонкие бордеры.
- **Цвет = смысл.** Зелёный — «в работе/успех», красный — остаток/удаление/опасность, оранжевый/синий — валютные пилюли, индиго — основное действие. Цвет не для украшения.
- **ПК-первый, телефон-адаптив.** Основной сценарий — десктоп (широкие таблицы, side-panel). На телефоне таблицы → карточки, side-panel → fullscreen, сайдбар → выезжающий.
- **Состояния всегда.** Каждый экран имеет загрузку (скелетон), пусто (иллюстрация + текст), ошибку (тост).
- **Backend — источник правды.** Все суммы/остатки/итоги считает бэкенд; фронт только отображает. Права тоже на бэке (UI лишь прячет кнопки).

---

## 2. Дизайн-токены

### 2.1 Тёмная тема (основная, по скриншотам)

```css
:root[data-theme="dark"]{
  /* Поверхности */
  --bg:           #0E1116;   /* канвас приложения */
  --sidebar:      #0B0E13;   /* сайдбар (чуть темнее фона) */
  --surface:      #161A22;   /* карточки, панели, плитки */
  --surface-2:    #1C212B;   /* инпуты, ховер-строки, вложенные блоки */
  --surface-3:    #232A36;   /* активный ховер, выделение */

  /* Бордеры / разделители */
  --border:       #232A36;
  --border-soft:  #1B212B;
  --track:        #2A3140;   /* фон прогресс-баров */

  /* Текст */
  --text:         #E6E9EF;   /* основной */
  --muted:        #8A93A3;   /* вторичный */
  --faint:        #5C6473;   /* подписи, плейсхолдеры */

  /* Акценты */
  --primary:      #5B5FE9;   /* индиго — основное действие, активный таб */
  --primary-d:    #4B4FD6;   /* hover */
  --primary-bg:   rgba(91,95,233,.14);

  --success:      #22C55E;   /* «В работе», успех */
  --success-bg:   rgba(34,197,94,.12);
  --danger:       #EF4444;   /* удалить, опасность */
  --danger-bg:    rgba(239,68,68,.12);
  --remain:       #E5736E;   /* красноватый «Остаток» */
  --warning:      #F59E0B;   /* оранжевая пилюля CNY */
  --warning-bg:   rgba(245,158,11,.14);
  --info:         #3B82F6;   /* синяя пилюля, тоггл «в оплатах» */
  --info-bg:      rgba(59,130,246,.14);

  /* Форма */
  --r:14px; --r-sm:10px; --r-xs:8px;
  --shadow:    0 1px 2px rgba(0,0,0,.4);
  --shadow-lg: 0 12px 32px rgba(0,0,0,.5);
}
```

### 2.2 Светлая тема (по переключателю — наследие текущего прототипа)

```css
:root[data-theme="light"]{
  --bg:#F1F5F9; --sidebar:#0F172A; --surface:#FFFFFF; --surface-2:#F8FAFC; --surface-3:#EEF2FF;
  --border:#E2E8F0; --border-soft:#EDF1F6; --track:#E2E8F0;
  --text:#0F172A; --muted:#64748B; --faint:#94A3B8;
  --primary:#4F46E5; --primary-d:#4338CA; --primary-bg:#EEF2FF;
  --success:#059669; --success-bg:#ECFDF5; --danger:#DC2626; --danger-bg:#FEF2F2;
  --remain:#DC2626; --warning:#D97706; --warning-bg:#FFFBEB; --info:#2563EB; --info-bg:#EFF6FF;
  --r:14px; --r-sm:10px; --r-xs:8px;
  --shadow:0 1px 3px rgba(15,23,42,.06); --shadow-lg:0 10px 30px rgba(15,23,42,.08);
}
```

> Переключатель темы — внизу сайдбара (кнопка «☀ Светлая / 🌙 Тёмная»), как на скриншоте. Выбор хранится в `localStorage`, ставится атрибут `data-theme` на `<html>`.

### 2.3 Типографика

- Шрифт: **Inter** (system-ui fallback).
- Заголовок раздела (H1): 22px / 700.
- Заголовок страницы заказа (H2): 24px / 800.
- Заголовок панели (H3): 15px / 700.
- Тело: 13–14px / 400–500.
- Числа в таблицах: 13px, цифры — `font-variant-numeric: tabular-nums` (ровные колонки сумм).
- Подписи/мета: 11–12px / 500, цвет `--muted`/`--faint`, иногда UPPERCASE с трекингом .3px (шапки таблиц).

### 2.4 Сетка и отступы

- Шкала отступов: 4 / 8 / 12 / 16 / 18 / 24 / 32.
- Контент: паддинг 24×32 (десктоп), 16 (моб). Между блоками — gap 24.
- Сайдбар: ширина 248px.
- Радиусы: карточки/панели 14px, кнопки/инпуты/чипы 10px, мелкие пилюли 999px.

### 2.5 Иконки

Один набор тонких иконок (рекоменд. **lucide-react**): папка, плюс, карандаш (edit), корзина (delete, красная), стрелка-назад, фильтр, поиск, загрузка файла, скачать (Excel), солнце/луна, чек, крестик, скрепка, колокол (ТГ-уведомление).

---

## 3. Каркас приложения (App Shell)

```
┌──────────────┬─────────────────────────────────────────────┐
│  SIDEBAR     │  CONTENT                                      │
│  248px       │  ┌─ H1 раздела ───────────── [+ действие] ─┐ │
│              │  │                                          │ │
│  лого/титул  │  │  toolbar: фильтры / сортировка           │ │
│  ─────────   │  │                                          │ │
│  nav-пункты  │  │  основной контент раздела                │ │
│              │  │                                          │ │
│  ─────────   │  └──────────────────────────────────────────┘ │
│  ☀ тема  ⟳   │                                               │
└──────────────┴─────────────────────────────────────────────┘
```

### Сайдбар
- Сверху: блок-заголовок («Управление заказами» + подпись, как «Тестовый клиент» на скрине → здесь имя текущего пользователя/роль).
- Навигация (пункты зависят от роли):
  - **Заказы** (folder)
  - **Запросы на оплату** (счёт/карта)
  - **Логистика** (грузовик)
  - **База данных** (база) → подпункты Пользователи / Клиенты / Поставщики (Пользователи только для админа)
  - **Аналитика** (график) — с пометкой `скоро`, неактивна
- Активный пункт: индиго-подсветка с лёгким свечением (как «Проекты» на скрине — рамка/заливка).
- Низ сайдбара: переключатель темы + кнопка обновить/выход; ник пользователя и роль.

### Адаптив сайдбара
- ≤760px: сайдбар скрыт, открывается бургером из топбара, затемнение-scrim, кнопка-крестик.

---

## 4. Каталог компонентов

| Компонент | Описание / стиль по скринам |
|-----------|------------------------------|
| **Кнопка primary** | Индиго заливка, белый текст, радиус 10, лёгкая тень-свечение. «+ Создать заказ», «+ Добавить», «+ Создать запрос». |
| **Кнопка ghost** | Прозрачная/`--surface`, бордер `--border`, текст `--text`. «Excel», «Редактировать», «Убрать выбор». |
| **Кнопка danger** | Иконка-корзина красная (`--danger`), на ховер фон `--danger-bg`. |
| **Статус-бейдж** | Пилюля с цветным текстом и тонкой рамкой того же цвета, фон полупрозрачный. `В работе`=зелёный, `Завершён`=индиго/серый, `Отменён`=красный/серый. |
| **Валютная пилюля** | Маленький прямоугольный бейдж: `CNY` оранжевый (`--warning-bg`), `RUB`/`USD` — синий/нейтральный. Ставится у строки товара/заявки. |
| **Чип-фильтр (таб-фильтр)** | Группа кнопок «В работе / Закрытые / Все» — активная индиго-заливка, остальные прозрачные. Радиус 10. |
| **Дропдаун-сортировка** | `--surface` + бордер, текст + шеврон. «Дата ↓ (новые)», «По дате», «↓ Убыв.». |
| **Плитка заказа (ocard)** | Карточка `--surface`, бордер, радиус 14. Сверху: `№22` (мелко, `--faint`) + статус-бейдж справа. Заголовок (название/клиент). Описание (`--muted`, либо «нет описания» `--faint`). Разделитель. Низ: иконки edit + delete. Ховер: подъём + бордер индиго. |
| **Вкладки заказа (tabs)** | Горизонтальные, нижний бордер; активная — индиго-подчёркивание + индиго-текст. |
| **Таблица данных** | Шапка: `--surface-2`, UPPERCASE мелкие подписи. Строки: разделитель `--border`, ховер `--surface-2`. Числа tabular-nums, выравнивание по правому краю. Футер с итогами — жирная верхняя граница, фон `--surface-2`. |
| **Тоггл-переключатель** | Синий активный (`--info`) — как «В оплатах» в таблице номенклатуры. |
| **Карточка-заявка (req)** | Блок в правой колонке: валютная пилюля + название, пилюля приоритета («Обычно»=синий, «Срочно»=красный, «Низкий»=серый). Строки: Всего / Фактически оплачено / Остаток. Прогресс-бар оплаты снизу. |
| **Прогресс-бар** | Тонкий (6px), трек `--track`, заливка индиго (или зелёная при 100%). |
| **Боковая панель деталей (side-panel)** | Выезжает справа при клике на строку/запись (товар, заявка, логистика, ДиР). На моб — fullscreen. Содержит полные данные + действия. |
| **Блок заметок** | Список заметок (автор + дата + текст) + поле «Новая заметка…» с кнопкой `+`. Пустое состояние «Нет заметок» с иконкой. |
| **Модалка/форма** | Затемнение + центр-карточка `--surface`. Поля: label сверху, инпут `--surface-2` + бордер, фокус — бордер индиго. Кнопки внизу справа (ghost «Отмена» + primary «Сохранить»). |
| **Аплоадер файлов** | Drag&drop зона (пунктир `--border`), превью списка, размер/лимит, удаление. Лимиты подписаны (напр. «до 3 файлов × 5 МБ»). |
| **Пустое состояние** | Иконка 48px + заголовок + поясняющий текст, по центру, пунктирная рамка. |
| **Тост** | Правый верх/низ: успех (зелёный), ошибка (красный), инфо. Авто-скрытие. |
| **Пагинация** | Низ списка: номера страниц + ‹ ›; активная — индиго. |
| **Скелетон** | Серые плейсхолдеры блоков на время загрузки (пульсация). |

---

## 5. Поэкранный план

### 5.1 Экран логина
- Центрированная карточка `--surface` на тёмном фоне. Лого сверху.
- Поля: Логин, Пароль (с «показать»). Кнопка «Войти» (primary, на всю ширину).
- Ошибка неверных кредов — красный текст под формой.
- Без регистрации (аккаунты заводит админ).

### 5.2 Раздел «Заказы» — список (главный экран) — *см. скрин 1*
- H1 «Заказы», справа кнопка **«+ Создать заказ»** (primary).
- Тулбар: чип-фильтры статуса **В работе / Завершённые / Все** + дропдаун сортировки «Дата ↓ (новые)». Плюс фильтры: **клиент**, **менеджер** (у менеджера в фильтре клиентов — только его клиенты).
- **Сетка плиток** (auto-fill, мин. 300px, 4 кол. на широком). Плитка: `№`, статус-бейдж, название/клиент, детали, разделитель, иконки edit/delete.
  - Видимость: админ/наблюдатель — все заказы; менеджер — только свои.
  - Удаление с плитки — только если у заказа нет связей (иначе 409 + тост).
- **Пагинация** снизу.
- Клик по плитке → детальная страница заказа.
- Создание: модалка-форма (детали, клиент, менеджер=текущий по умолчанию, дата) → после сохранения переход на страницу заказа.

### 5.3 Страница заказа (гибрид-вкладки) — *см. скрин 2*

**Шапка:**
- Слева: стрелка-назад, мелкий `№22`, крупный заголовок (название/клиент).
- Справа: статус-бейдж (`В работе`), кнопка **Excel** (экспорт), **Редактировать**, корзина (удалить).
- Строка-мета под заголовком: клиент, менеджер, дата создания, валюта заказа.

**Блок прибыли (Итоги)** — горизонтальная панель сразу под шапкой:
- Карточки-слагаемые: Доходы − Закупки − Логистика − Расходы = **Чистая прибыль** (зелёный total) + % прибыли + дней в обработке.
- Если не выполнено условие (не весь товар отправлен И принят) — вместо чисел плашка-подсказка «Итоги посчитаются после приёмки всей логистики» (warning-hint).

**Вкладки:** `Товары · Запросы на оплату · Логистика · ДиР`. Активная — индиго-подчёркивание.

#### Вкладка 1 — Товары
- Тулбар панели: «Убрать выбор» (ghost), «+ Добавить» (primary).
- **Таблица номенклатуры** (как на скрине): Наименование · Кол-во · Итого · Выставлено · Оплачено · Остаток · «В оплатах» (тоггл). Остаток — красноватый (`--remain`).
- **Футер-итоги** по валютам: «CNY: Итого ¥… · Выставлено · Оплачено · Общий остаток» + вторая строка «Остаток по счетам · К выставлению · Комиссия».
- Клик по строке → **side-panel товара**: наименование, детали, поставщик, кол-во, цена, валюта, итого; суммы счетов/оплачено/остаток; отправлено + трекинги; 1 фото. Кнопки: редактировать / удалить (удаление блокируется, если есть запрос или логистика по товару).
- Добавление — модалка: наименование, детали, кол-во, цена, валюта, поставщик, фото.

#### Вкладка 2 — Запросы на оплату (закупки)
- «+ Создать запрос» → модалка: список всех товаров заказа с галочками; при отметке сумма = остатку (редактируемо); итог снизу; реквизиты, детали, приоритет; файлы (до 3×5 МБ). Запрет смешивать валюты; контроль превышения остатка.
- Список **карточек-заявок** (как в правой колонке скрина): валютная пилюля + название, пилюля приоритета, «Всего», «Фактически оплачено», «Остаток», прогресс-бар. Фильтры сверху: статус, по дате, направление сортировки.
- Создание запроса → **ТГ-уведомление** в группу клиента.
- Клик → **side-panel заявки**: товары, остаток к оплате, реквизиты, файлы; кнопки «Копировать информацию», редактировать, удалить (нельзя при наличии оплаты). **«Внести оплату»** — форма: файл, сумма, курс (вручную), примечание. Частичные оплаты, пересчёт остатка.

#### Вкладка 3 — Логистика
- «+ Создать логистику» → форма: товар + кол-во (контроль остатка отправки), трекинг (текст-код), дата отправки, файл накладной, детали.
- Список записей логистики (статус, трекинг, товары, дата отправки, дата приёмки, расход).
- Клик → **side-panel логистики**: статус, № заказа, трекинг, список товара, дата отправки; блок приёмки (дата/сумма/валюта/курс/примечание); комментарии (могут все, на любом статусе).
  - Кнопки сверху: Редактировать (менеджер — только «в дороге»), Удалить (только «в дороге»), Копировать.
  - Кнопки снизу: «Уведомить о получении» (ТГ), **«Принять»** (только админ) → форма приёмки → статус «принят», данные замораживаются; затем кнопка превращается в **«Распринять»** (откат в «в дороге»).
- Статусы: В дороге / Принят / Отменён (цветовые бейджи).

#### Вкладка 4 — ДиР (Доходы и Расходы)
- Две кнопки: «+ Добавить доход» / «+ Добавить расход».
- Форма: сумма, валюта, курс (вручную), детали. Фиксируются автор и дата.
- Общая таблица с колонкой типа (доход/расход), сумма, валюта, курс, дата.
- Раздельные итоги по доходам и расходам внизу.
- Клик → side-panel записи (с автором). Можно удалить, нельзя редактировать.

**Заметки заказа** — блок внизу страницы (на всех вкладках или в правой части): список (автор+дата+текст) + поле добавления. Наблюдатель тоже может оставлять заметки/комментарии.

### 5.4 Сводка «Запросы на оплату»
- Один экран-таблица всех запросов из всех заказов.
- Фильтры: работник, клиент; сортировка по дате (туда-обратно).
- Колонки: № заказа (**ссылка**), менеджер, клиент, дата создания, сумма запроса, остаток.
- Клик → side-panel заявки (та же логика, внесение оплат прямо отсюда).

### 5.5 Сводка «Логистика» (одна страница)
- Все логистики в одном месте; фильтры: статус, клиент, менеджер + **поиск** (по трекингу/№).
- Колонки: трекинг, № заказа (**ссылка**), клиент, менеджер, дата отправки, дата приёмки, расход.
- Клик → side-panel логистики: редактировать / копировать / принять / распринять / менять статус (как из заказа).
- Цель: коллега на складе видит весь товар в пути, не прыгая по заказам.

### 5.6 Раздел «База данных»
Вкладки: **Пользователи** (только админ) · **Клиенты** · **Поставщики**.
- **Пользователи:** таблица (ФИО, логин, роль, активен). Создание (логин/пароль/роль), смена роли, деактивация. Передача аккаунта уволенного = смена ФИО/пароля.
- **Клиенты:** таблица (код, ФИО, описание, ссылка ТГ-группы). Уникальность кода; удаление запрещено при наличии заказов.
- **Поставщики:** таблица (название, контакты/детали). CRUD.
- Стиль таблиц — общий тёмный (см. компонент «Таблица данных»), формы — модалки.

### 5.7 Раздел «Аналитика»
- Пункт в сайдбаре с пометкой `скоро`, экран-заглушка (пустое состояние «Раздел в разработке»). БД проектируется заранее под метрики; UI сейчас не делаем.

---

## 6. Адаптив

| Брейкпоинт | Поведение |
|------------|-----------|
| **>1024px (десктоп)** | Полный сайдбар; плитки 3–4 в ряд; таблицы как есть; side-panel — выезжает справа поверх (≈420px). |
| **≤1024px (планшет)** | Плитки 2 в ряд; контент-паддинг уже; сайдбар остаётся. |
| **≤760px (телефон)** | Сайдбар → бургер + scrim; плитки в 1 колонку; **таблицы → карточки** (каждая строка = карточка с подписями колонок); side-panel → fullscreen; шапка заказа — кнопки в строку с переносом; блок прибыли — вертикально. |

---

## 7. Связь с требованиями и сборкой

- Каждый экран выше реализует соответствующий раздел [ТЗ](<ТЗ на проект подсчета заказов (обновленное).md>) (§5–§17).
- Порядок реализации — по фазам [ПЛАН_РАЗРАБОТКИ.md](ПЛАН_РАЗРАБОТКИ.md): дизайн-токены и компоненты вводятся в Фазе 0.6 (скелет фронта) и наполняются по мере экранов (Фазы 4–10).
- Рекомендация: вынести токены и каталог компонентов в общий UI-слой (`frontend/src/ui/`) на старте, чтобы все экраны собирались из готовых блоков и тема переключалась одним атрибутом.

## 8. Следующий шаг (предложение)

Переключить визуальный **прототип** (`prototype/`) со светлой на тёмную тему по этим токенам и собрать в нём 2 эталонных экрана (список заказов + страница заказа с вкладками), чтобы был кликабельный референс перед версткой на React. Скажи — и сделаю.
